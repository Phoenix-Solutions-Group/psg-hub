.DS_Store

# local credentials and environment
.env
.env.*
!.env.example
*.pem
*.key
*credential*.json
*credentials*.json
*service-account*.json
bigQuery-*.json

# dependencies and build outputs
node_modules/
.next/
.turbo/
out/
build/
dist/
coverage/
*.tsbuildinfo
next-env.d.ts
__pycache__/
.pytest_cache/
.venv/
*.pyc
apps/psg-hub/.playwright-browsers/

# pnpm
.pnpm-store/

# local/editor state
.vscode/
.idea/
.vercel/
# graphify knowledge-graph output (local artifact, any depth)
graphify-out/
supabase/.temp/
*/supabase/.temp/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.vercel

# workspace consolidation (Phase 1) — archived + independently-versioned projects
# NOTE: root-anchored (leading /) so nested archive/ dirs (e.g. .paul/handoffs/archive/) stay tracked
# /archive/ holds nested repos + 2.9G accidents.db; do NOT track
/archive/
# separate active projects with their own .git (untracked in v0.1)
/psg-import/
/api-psghub/
# data lake (D14 untouched) — large, not source
/psg-data-lake/
# local agent state (any depth)
.claude/
# per-issue git worktrees (PSG-87). Default root is a SIBLING dir (../psg-worktrees,
# outside this tree); these patterns are defensive in case PSG_WT_ROOT is set in-tree.
/psg-worktrees/
.worktrees/
# deferred classification (Phase 1 close)
/psg-agentic-os-dev-packet.docx
# real pilot exports contain live customer PII (names/addresses/emails/phones).
# NEVER commit. Used locally only to validate the in-hub importer (PSG-51).
/docs/Filemaker Exports/
/docs/psg/
