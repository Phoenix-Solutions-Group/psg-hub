---
phase: 19-secure-document-review
plan: "03"
type: execute
wave: 1
depends_on: ["19-01"]
files_modified:
  - workers/document-converter/Dockerfile
  - workers/document-converter/convert.mjs
  - workers/document-converter/verify.mjs
  - workers/document-converter/README.md
  - workers/document-converter/fixtures/
autonomous: true
requirements: [SDR-02, SDR-10]
decisions: [D-03, D-04, D-17, D-18, D-19]
must_haves:
  truths:
    - "D-04/D-18: the pinned worker converts hostile input offline to inert page PNG/text bundles and cannot contact arbitrary networks."
    - "D-03/D-17: PDF, DOCX, and self-contained HTML pass extension, signature, structure, expansion, page, time, and output limits."
    - "D-19: the worker emits no editable document model, active HTML, OCR, drawings, or anchor migration."
  artifacts:
    - path: "workers/document-converter/convert.mjs"
      provides: "Fail-closed PDF/DOCX/HTML validation and deterministic page-bundle conversion"
    - path: "workers/document-converter/Dockerfile"
      provides: "Digest-pinned custom image with pinned Chromium, LibreOffice, Poppler, and file tooling"
    - path: "workers/document-converter/verify.mjs"
      provides: "Runnable valid/malicious corpus self-check"
  key_links:
    - from: "convert.mjs"
      to: "manifest.json + page PNG/text files"
      via: "bounded offline converter commands"
      pattern: "source digest and per-page dimensions/checksums"
---

<objective>Build the proved, pinned, deny-all-compatible converter image without adding an application runtime dependency.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Package the pinned offline converter image</name>
  <read_first>
    .paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md
    .paul/phases/19-secure-document-review/19-RESEARCH.md
    .paul/phases/19-secure-document-review/19-PATTERNS.md
    workers/report-renderer/Dockerfile
  </read_first>
  <files>workers/document-converter/Dockerfile, workers/document-converter/README.md</files>
  <action>
    Create the custom image from the exact approved base digest and tool versions recorded in the Wave 0 proof. Install only the pinned Chromium, LibreOffice, Poppler, `file`, and required fonts/system libraries; do not add an npm runtime package or reuse the long-lived report renderer. Run as a non-root user with a fixed `/work` contract. Document build, immutable digest publication, input/output paths, CPU/time/output ceilings, and the required Vercel `deny-all` policy. The image receives document bytes only and no PSG secret.
  </action>
  <acceptance_criteria>
    - Base image and converter tools are pinned; no `latest` tag exists.
    - The runtime contract requires deny-all networking and a fresh sandbox per input.
  </acceptance_criteria>
  <verify><automated>rg -n "sha256:|USER |LibreOffice|Poppler|Chromium|deny-all|/work" workers/document-converter/Dockerfile workers/document-converter/README.md &amp;&amp; ! rg -n ":latest" workers/document-converter</automated></verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded format validation and inert normalization</name>
  <read_first>
    .paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md
    .paul/phases/19-secure-document-review/19-VALIDATION.md
    workers/document-converter/Dockerfile
  </read_first>
  <files>workers/document-converter/convert.mjs, workers/document-converter/verify.mjs, workers/document-converter/fixtures/</files>
  <action>
    Implement the CLI contract with Node standard library process/file APIs. Validate the approved input ceiling, extension/signature agreement, PDF structure/encryption, DOCX ZIP integrity/required entries/expanded-byte ratio, and self-contained UTF-8 HTML. For HTML, inject restrictive CSP, disable JavaScript before load, reject remote/file resources, and rely on deny-all egress as the outer control. Convert HTML/DOCX to PDF, then rasterize and extract plain text with fixed process timeouts and the approved page/output ceilings. Emit a versioned manifest containing source digest, page count, dimensions, image/text filenames, and checksums. On any error, exit non-zero with a generic code and leave no accepted manifest. The self-check must cover every valid and malicious fixture named in 19-VALIDATION.
  </action>
  <acceptance_criteria>
    - Valid representative PDF/DOCX/HTML produce deterministic validated bundles.
    - Mismatch, malformed/encrypted input, archive/page bombs, active HTML, remote resources, timeout, and invalid output fail closed.
  </acceptance_criteria>
  <verify><automated>docker build -t psg-document-converter:test workers/document-converter &amp;&amp; docker run --rm --network=none --read-only --tmpfs /work:rw,noexec,nosuid,size=256m psg-document-converter:test node /opt/document-converter/verify.mjs</automated></verify>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Disposition | Mitigation |
|---|---|---|---|
| T-19-03-01 | Elevation / Information Disclosure | mitigate | Non-root ephemeral image, deny-all egress, no app secrets, inert output only |
| T-19-03-02 | Denial of Service | mitigate | Byte/expansion/page/output/process/sandbox ceilings |
| T-19-03-03 | Tampering | mitigate | Digest-pinned image and manifest/source/page checksums |
</threat_model>
<verification>Build the custom image and run its complete corpus with Docker network disabled.</verification>
<success_criteria>The converter deterministically produces only inert page bundles and fails closed on the malicious corpus without an added app dependency.</success_criteria>
<output>Create `.paul/phases/19-secure-document-review/19-03-SUMMARY.md`.</output>
