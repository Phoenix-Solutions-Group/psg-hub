---
phase: 19-secure-document-review
plan: "02"
type: tdd
wave: 1
depends_on: ["19-01"]
files_modified:
  - supabase/migrations/20260728000000_document_reviews.sql
  - supabase/tests/document_review_rls.sql
autonomous: true
requirements: [SDR-03, SDR-04, SDR-05, SDR-07, SDR-09, SDR-10, SDR-12]
decisions: [D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-13, D-14, D-16, D-20, D-21, D-22]
must_haves:
  truths:
    - "D-20/D-21/D-22: local Supabase owns review state, digest-only guests, private object metadata, and RLS; production remains untouched."
    - "D-11/D-13/D-14: submissions and versions are immutable, reopen creates a new generation, and anchors never move between versions."
    - "D-07/D-08/D-09/D-10: exact assigned recipients alone can mutate normalized pin comments while their generation is editable."
    - "D-16: upload, invitation, revocation, submission, reopen, and version creation append audit records."
  artifacts:
    - path: "supabase/migrations/20260728000000_document_reviews.sql"
      provides: "Seven-table schema, private bucket declaration, RLS, constraints, immutable guards, and atomic workflow functions"
    - path: "supabase/tests/document_review_rls.sql"
      provides: "Local pgTAP authorization, immutability, transition, and dedupe matrix"
  key_links:
    - from: "workflow RPCs"
      to: "comments, submissions, audit, notifications"
      via: "one local Postgres transaction with row locks"
      pattern: "snapshot -> lock -> audit -> notification"
---

<objective>
Create the local-only relational authority for exact-recipient review workflow, immutable history, audit, notification dedupe, and private Storage posture.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Write the failing local SQL authorization and state contract</name>
  <read_first>
    .paul/phases/19-secure-document-review/19-CONTEXT.md
    .paul/phases/19-secure-document-review/19-RESEARCH.md
    .paul/phases/19-secure-document-review/19-PATTERNS.md
    .paul/phases/06-rbac-rls-spine/PROTOCOL-migration-safety.md
    .paul/phases/06-rbac-rls-spine/CHECKLIST-rls-review.md
    supabase/migrations/20260602163208_app_user_roles_security_profiles.sql
    supabase/migrations/20260602163210_rbac_helpers.sql
    supabase/migrations/20260613000000_monthly_reports_claim.sql
  </read_first>
  <files>supabase/tests/document_review_rls.sql</files>
  <action>
    Create a pgTAP contract for seven tables: reviews, immutable versions, recipients, draft comments, immutable submissions, append-only audit events, and notification ledger. Cover capability admin, assigned authenticated recipient, active guest service path, unrelated user, expired/revoked guest, direct object-policy denial, ratio/page/state checks, exactly-one authenticated-or-guest identity, token-digest uniqueness, comment lock after submit, ordered atomic snapshot, reopen generation, immutable version/submission/audit rows, and notification dedupe. Include plan/finish and transaction rollback. Do not connect to a linked database.
  </action>
  <acceptance_criteria>
    - Every SDR-03/04/05/07/09/10/12 database behavior has a named pgTAP assertion.
    - The test explicitly rejects cross-recipient reads/mutations and update/delete of immutable history.
  </acceptance_criteria>
  <verify><automated>supabase test db --local supabase/tests/document_review_rls.sql; test $? -ne 0</automated></verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the additive schema, RLS, immutable guards, and atomic functions</name>
  <read_first>
    supabase/tests/document_review_rls.sql
    .paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md
    .paul/phases/06-rbac-rls-spine/PROTOCOL-migration-safety.md
    .paul/phases/06-rbac-rls-spine/CHECKLIST-rls-review.md
  </read_first>
  <files>supabase/migrations/20260728000000_document_reviews.sql</files>
  <action>
    Author one additive, idempotent, reversible transaction. Create the seven research tables with indexed foreign keys, named checks, generated immutable version/object prefixes, normalized ratios in [0,1], unique version/submission numbers, unique SHA-256 token digests, and unique notification dedupe keys. Add the `secure_document_review_admin` capability policy using `private.current_user_has_fn`; authenticated recipient SELECT policies must bind `(select auth.uid())` to the exact recipient/version and never imply admin rights from shop membership. Keep guest access and all writes service-only. Create a private local `document-reviews` bucket record with no anon/authenticated `storage.objects` policy. Add non-public service-only functions for create-version/invite/revoke/comment mutation/submit/reopen/notification claim; submit must row-lock, order comments by page/y/x/created_at, insert an immutable JSON snapshot, advance generation/state, append audit, and enqueue the deduped notice atomically. Triggers reject update/delete of versions, submissions, and audit rows and reject comment mutations outside draft/reopened state. Use no public SECURITY DEFINER function and do not touch sibling tables or policies.
  </action>
  <acceptance_criteria>
    - All seven tables enable RLS in this migration; service-only tables have zero browser policies.
    - State transitions are race-safe and historical rows cannot be changed or deleted.
    - Bucket and schema objects are local definitions only; no remote command appears.
  </acceptance_criteria>
  <verify><automated>rg -n "enable row level security|current_user_has_fn|document-reviews|snapshot|notification|audit" supabase/migrations/20260728000000_document_reviews.sql</automated></verify>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: Apply and prove the schema locally before any higher-layer verification</name>
  <read_first>
    supabase/migrations/20260728000000_document_reviews.sql
    supabase/tests/document_review_rls.sql
    .paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md
  </read_first>
  <files>supabase/migrations/20260728000000_document_reviews.sql, supabase/tests/document_review_rls.sql</files>
  <action>
    Confirm local Supabase URLs contain localhost/127.0.0.1 and refuse otherwise. Run `supabase db reset --local`, then the phase pgTAP file. Fix schema/test defects until both pass. This task blocks plans 19-05 and higher layers. Never run `supabase db push`, `--linked`, MCP apply, or any command against project `gylkkzmcmbdftxieyabw`.
  </action>
  <acceptance_criteria>
    - Local reset and all pgTAP assertions pass.
    - Command history/evidence contains no linked or production mutation.
  </acceptance_criteria>
  <verify><automated>supabase db reset --local &amp;&amp; supabase test db --local supabase/tests/document_review_rls.sql</automated></verify>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Disposition | Mitigation |
|---|---|---|---|
| T-19-02-01 | Spoofing / Information Disclosure | mitigate | Exact recipient/profile policies and default-deny guest/browser paths |
| T-19-02-02 | Tampering / Repudiation | mitigate | Row locks, immutable triggers, append-only audit, atomic snapshots |
| T-19-02-03 | Elevation | mitigate | Private capability helper, no public SECURITY DEFINER, fixed search paths |
| T-19-02-04 | Information Disclosure | mitigate | Private bucket and no direct authenticated object policy |
</threat_model>

<verification>`supabase db reset --local` and `supabase test db --local supabase/tests/document_review_rls.sql` pass before downstream work.</verification>
<success_criteria>The local database alone enforces the complete recipient, state, history, audit, dedupe, and Storage-privacy contract with production untouched.</success_criteria>
<output>Create `.paul/phases/19-secure-document-review/19-02-SUMMARY.md`.</output>
