---
phase: 19-secure-document-review
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - .paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md
autonomous: false
requirements: [SDR-02, SDR-10]
decisions: [D-02, D-03, D-04, D-17, D-18, D-19]
must_haves:
  truths:
    - "D-02/D-18: Phase 19 does not proceed until a non-production Vercel Sandbox proves entitlement, a pinned custom image, deny-all egress, and create/write/run/read/stop cleanup."
    - "D-03/D-04/D-17: representative PDF, DOCX, and self-contained HTML normalize to inert page images and text within proven 4 MiB, 100-page, and two-minute ceilings."
    - "D-19: the proof demonstrates page bundles only; it does not introduce document editing, active browser rendering, or another conversion provider."
  artifacts:
    - path: ".paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md"
      provides: "Recorded PASS/FAIL evidence, accepted defaults, fidelity contact-sheet locations, entitlement/cost/region, image digest, egress results, and lifecycle cleanup"
  key_links:
    - from: "temporary Wave 0 smoke workspace"
      to: "Vercel Sandbox"
      via: "custom image digest with networkPolicy deny-all and explicit stop in finally"
      pattern: "create -> write -> run -> read -> stop"
---

<objective>
Prove the only uncertain external trust boundary before any application dependency or converter architecture is committed.

Purpose: Make the 4 MiB/100-page/7-day defaults, representative-format fidelity, and Vercel Sandbox isolation executable gates rather than assumptions.
Output: A signed-off `19-SANDBOX-PROOF.md`; a failure stops the phase for replanning without selecting another provider.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Prove Sandbox entitlement, pinned-image isolation, and lifecycle</name>
  <read_first>
    .paul/phases/19-secure-document-review/19-CONTEXT.md
    .paul/phases/19-secure-document-review/19-RESEARCH.md
    .paul/phases/19-secure-document-review/19-VALIDATION.md
  </read_first>
  <files>.paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md</files>
  <action>
    Work in a `mktemp -d` directory outside the repository. Confirm the repo's package.json and pnpm-lock.yaml hashes before and after the smoke so `@vercel/sandbox` is not installed in PSG Hub. In the temporary workspace only, use the audited `@vercel/sandbox@2.9.0` package to prove account entitlement and a custom OCI image referenced by immutable digest. Create a sandbox with `networkPolicy: "deny-all"`, write a fixture, run a deterministic command, read the result, and stop the sandbox in `finally`. Prove HTTP, DNS, WebSocket, and `file://` escape attempts do not succeed; prove no Supabase, SendGrid, or PSG application secret is present. Record account/region availability, observed duration, cost estimate, image digest, command results, and stop confirmation. If entitlement, custom images, deny-all policy, or lifecycle cleanup fails, mark FAIL and do not continue to Task 2 or any later plan.
  </action>
  <acceptance_criteria>
    - PSG Hub package files are byte-identical before and after the smoke.
    - Evidence shows create, write, run, read, and stop succeeded against the pinned image with deny-all egress.
    - The proof contains no secret values and no production Supabase, Storage, email, deploy, or data mutation.
  </acceptance_criteria>
  <verify>
    <automated>git diff --exit-code -- package.json pnpm-lock.yaml &amp;&amp; rg -n "PASS|image digest|deny-all|create|write|run|read|stop|cost|region" .paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Prove limits, fidelity, inert output, and accessibility boundary</name>
  <read_first>
    .paul/phases/19-secure-document-review/19-UI-SPEC.md
    .paul/phases/19-secure-document-review/19-RESEARCH.md
  </read_first>
  <files>.paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md</files>
  <action>
    Using one operator-approved representative PDF, DOCX, and self-contained HTML file, run the proved sandbox path and record source bytes, rendered pages, duration, output bytes, extracted-text presence, and visual variance. Include one extracted-text page and one image-only page. Exercise the boundary corpus: 4 MiB accepted, oversized input rejected, 100 pages accepted, 101 pages rejected, remote HTML assets and active HTML behaviors blocked, malformed/encrypted PDF and malformed OOXML rejected. Produce contact-sheet file locations for human inspection. Evaluate the 7-day guest expiry as an operating-policy value and record the accepted value; do not create guest tokens or send invitations. If representative fidelity or any safety/limit check fails, mark FAIL and stop.
  </action>
  <acceptance_criteria>
    - The report records PASS/FAIL for PDF, DOCX, and self-contained HTML fidelity and every stated boundary.
    - Output is only page images, plain text, and a manifest; no uploaded source is browser-rendered.
    - The report records the accepted upload, page, processing, and guest-expiry defaults used by later plans.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "PDF|DOCX|self-contained HTML|4 MiB|100 pages|101 pages|7-day|image-only|extracted text|active HTML|PASS|FAIL" .paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Accept the Wave 0 proof or stop for replanning</name>
  <read_first>
    .paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md
  </read_first>
  <files>.paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md</files>
  <action>
    Present the evidence and representative contact sheets. Approval is permitted only when every required line is PASS and the operator accepts the recorded fidelity, defaults, entitlement, cost, and region. On any failure or rejection, stop Phase 19 and revise the plans; do not install `@vercel/sandbox` in the app, select another provider, or continue with schema/converter implementation.
  </action>
  <acceptance_criteria>
    - The operator records `APPROVED` or `REPLAN REQUIRED` in the proof.
    - Only `APPROVED` unblocks plans 19-02 and 19-03.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^Decision: (APPROVED|REPLAN REQUIRED)$" .paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Risk | Control |
|---|---|---|
| Representative upload -> converter | Hostile parser input, active HTML, resource exhaustion | Temporary pinned microVM, deny-all egress, no secrets, byte/page/time/output limits |
| Sandbox -> operator evidence | Secret leakage or false cleanup proof | Redacted report, explicit stop result, unchanged repo package hashes |

## STRIDE Threat Register
| Threat ID | Category | Disposition | Mitigation |
|---|---|---|---|
| T-19-01-01 | Elevation / Information Disclosure | mitigate | Prove deny-all custom-image isolation before any implementation dependency |
| T-19-01-02 | Denial of Service | mitigate | Prove 4 MiB, 100-page, expansion, output, and timeout ceilings |
| T-19-01-03 | Tampering | mitigate | Pin the image by digest and record fixture/source hashes |
</threat_model>

<verification>
Wave 0 is complete only with an operator-approved all-PASS proof, unchanged PSG Hub package files, and zero production contact.
</verification>

<success_criteria>
The phase has empirical permission to use the locked Sandbox architecture and proven shared defaults. Any failure leaves later plans blocked and triggers replanning without a silent provider swap.
</success_criteria>

<output>Create `.paul/phases/19-secure-document-review/19-01-SUMMARY.md` after approval.</output>
