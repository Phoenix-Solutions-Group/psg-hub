---
phase: 19-secure-document-review
plan: "04"
type: tdd
wave: 2
depends_on: ["19-03"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/lib/document-review/artifact.ts
  - src/lib/document-review/converter.ts
  - src/lib/document-review/__tests__/artifact.test.ts
  - src/lib/document-review/__tests__/converter.test.ts
autonomous: true
requirements: [SDR-02, SDR-10]
decisions: [D-04, D-17, D-18]
must_haves:
  truths:
    - "D-18: the app creates one deny-all sandbox per input, validates every output, and stops it in finally."
    - "D-04/D-17: only a bounded, checksum-verified inert artifact manifest can cross from the converter into persistence."
  artifacts:
    - path: "src/lib/document-review/artifact.ts"
      provides: "Zod artifact contract and server-owned proven limits"
    - path: "src/lib/document-review/converter.ts"
      provides: "Injected create/write/run/read/stop Sandbox adapter"
  key_links:
    - from: "converter.ts"
      to: "workers/document-converter/convert.mjs"
      via: "pinned image digest and fixed command contract"
      pattern: "networkPolicy deny-all"
---

<objective>Install the single research-approved application dependency and bind PSG Hub to the proved converter through a strict injected adapter.</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define the artifact and limit contract from Wave 0 evidence</name>
  <read_first>
    .paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md
    .paul/phases/19-secure-document-review/19-RESEARCH.md
    src/lib/report/schema.ts
  </read_first>
  <files>src/lib/document-review/artifact.ts, src/lib/document-review/__tests__/artifact.test.ts</files>
  <action>
    Export one Zod manifest schema and inferred types for schema version, source SHA-256, bounded page count, page dimensions, relative image/text filenames, and per-object checksums. Export the proven server-owned input/page/processing/output/guest-expiry values from the Wave 0 proof so routes and UI consume one source. Reject path traversal, duplicate pages, missing pages, absolute paths, invalid digests, unsupported schema versions, and values outside proven ceilings. Tests define valid and invalid manifests before implementation.
  </action>
  <acceptance_criteria>
    - Limits exactly equal the approved proof values.
    - Manifest parsing rejects every unsafe or inconsistent shape before persistence.
  </acceptance_criteria>
  <verify><automated>pnpm test -- src/lib/document-review/__tests__/artifact.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install the approved SDK and implement the lifecycle adapter</name>
  <read_first>
    .paul/phases/19-secure-document-review/19-RESEARCH.md
    .paul/phases/19-secure-document-review/19-SANDBOX-PROOF.md
    src/lib/report/render-client.ts
    src/lib/document-review/artifact.ts
  </read_first>
  <files>package.json, pnpm-lock.yaml, src/lib/document-review/converter.ts, src/lib/document-review/__tests__/converter.test.ts</files>
  <action>
    After confirming `Decision: APPROVED`, add exactly `@vercel/sandbox@2.9.0`; add no other provider, parser, annotation, or runtime package. Read the installed package types before coding. Implement an injected adapter for sandbox create, file write, command run, output read, and stop. Production defaults require the immutable image digest, `networkPolicy: "deny-all"`, approved timeout, no forwarded environment secrets, fixed input/output paths, non-zero exit handling, manifest validation, checksum verification for every returned object, and `stop()` in `finally`. Unit tests use fakes only and prove create/write/run/read/stop order, invalid manifest/checksum rejection, timeout/crash handling, and cleanup after every failure.
  </action>
  <acceptance_criteria>
    - The root dependency diff contains only `@vercel/sandbox@2.9.0`.
    - All result/error branches stop the sandbox and no test contacts Vercel.
  </acceptance_criteria>
  <verify><automated>pnpm test -- src/lib/document-review/__tests__/converter.test.ts &amp;&amp; pnpm typecheck &amp;&amp; node -e "const p=require('./package.json'); if(p.dependencies['@vercel/sandbox']!=='2.9.0') process.exit(1)"</automated></verify>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Disposition | Mitigation |
|---|---|---|---|
| T-19-04-01 | Tampering / Supply Chain | mitigate | Audited exact SDK version, lockfile, immutable converter image digest |
| T-19-04-02 | Information Disclosure / Elevation | mitigate | Deny-all, no forwarded secrets, schema/checksum validation, stop in finally |
| T-19-04-SC | Tampering | mitigate | No [ASSUMED]/[SUS] install; package legitimacy audit is present and approved |
</threat_model>
<verification>Targeted tests, typecheck, and dependency assertion pass.</verification>
<success_criteria>The app has one audited Sandbox dependency and a testable fail-closed adapter matching the proved lifecycle and artifact contract.</success_criteria>
<output>Create `.paul/phases/19-secure-document-review/19-04-SUMMARY.md`.</output>
