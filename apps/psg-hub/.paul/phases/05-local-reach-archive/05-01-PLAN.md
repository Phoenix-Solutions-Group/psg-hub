---
phase: 05-local-reach-archive
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Verify the local_reach active client outputs are fully and faithfully preserved on disk and the codebase is retired, then close Phase 5 (which completes milestone v0.1). On-disk preservation at the gitignored `archive/local_reach-outputs/` is the chosen mechanism (operator: lean close, on-disk only). No new artifact, no git changes to the outputs.
</objective>

<context>
@.paul/PROJECT.md
@.paul/ROADMAP.md
archive/local_reach-outputs/MANIFEST.md
</context>

<acceptance_criteria>
## AC-1: Outputs preserved + codebase retired (verified)
Given local_reach was archived in Phase 1 / 01-04 (D69)
When Phase 5 audits `archive/local_reach-outputs/` against its MANIFEST and confirms the source codebase is retired
Then the named client outputs (`tracys-research-v3`, `new-tracys-report-v2`, `tracys-local-reach-content`) exist with file counts/sizes matching the MANIFEST, the source `~/apps/projects/local_reach/` no longer exists, and the codebase is archived (gitignored under `/archive/`) — preservation confirmed sufficient for v0.3 BSM-agent-migration reference, with no new artifact created.
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Verify local_reach output preservation + retirement, then close</name>
  <files>(verification only — no file changes; archive/ is gitignored and stays on-disk per operator's lean-close choice)</files>
  <action>
    Confirm, read-only (the work was done in 01-04; this is the closing audit):
    - `archive/local_reach-outputs/` contains `tracys-research-v3` (5 files, ~420K), `new-tracys-report-v2` (5 files, ~1.1M), `tracys-local-reach-content` (index.html, ~76K), and `MANIFEST.md` — counts/sizes matching the MANIFEST.
    - Source `~/apps/projects/local_reach/` no longer exists (already relocated in Phase 1).
    - Codebase archived at `archive/local_reach/` and gitignored under `/archive/` — retired.
    - No client-output dir was missed: the 01-04 scan matched `tracys-research-*` / `new-tracys-report-*` and found no others; `archive/local_reach/output/www.tracysbodyshop.com` is scraped raw site data (correctly scoped out, not a client deliverable); Pine Ridge Coach Works research is folded inside `tracys-research-v3` (multi-shop batch, per MANIFEST).
    Do NOT move, commit, force-add, or modify the outputs — operator chose on-disk-only. `archive/` stays gitignored.
  </action>
  <verify>From repo root `/Users/schoolcraft_mbpro/apps/psg`: `ls archive/local_reach-outputs/{tracys-research-v3,new-tracys-report-v2,tracys-local-reach-content,MANIFEST.md}` all resolve; per-dir `find … -type f | wc -l` matches MANIFEST (5 / 5 / 1); `test ! -e ~/apps/projects/local_reach && echo retired`; `git check-ignore archive/local_reach-outputs` confirms gitignored (on-disk-only by design).</verify>
  <done>AC-1 satisfied: outputs preserved + faithful to MANIFEST, source gone, codebase retired, on-disk preservation confirmed sufficient.</done>
</task>
</tasks>

<output>
After completion, create compressed `.paul/phases/05-local-reach-archive/05-01-SUMMARY.md`. Phase 5 is the LAST v0.1 phase, so UNIFY triggers the milestone-complete transition (milestone "complete" = all 5 phases loop-closed; the v0.1→main merge stays operator-gated on the private submodule — NOT a shipped/merged claim).
</output>
