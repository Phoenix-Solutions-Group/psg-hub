---
phase: 11-ga4-gsc
plan: 04
type: execute
wave: 1
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - .paul/phases/11-ga4-gsc/11-04-GATE-BATCH.md            # the operator runbook (authored Task 1)
  - "(git) feature/11-ga4-gsc branch + ONE Phase-11 commit of the 11-01/11-02/11-03 trees (Task 2, NO push)"
autonomous: false   # Task 3 is checkpoint:human-action — the operator works the runbook against prod; the loop closes on REAL NUMBERS, not cron-200
---

<objective>
## Goal
Activate the entire Phase-11 build (11-01 OAuth foundation · 11-02 GA4 ingest · 11-03 GSC ingest) on prod and close
Phase 11 LIVE on REAL NUMBERS. This plan authors the operator runbook (`11-04-GATE-BATCH.md`) and lands the single
Phase-11 commit locally; the operator executes the runbook (2 prod migrations under PROTOCOL + the consent/API/site
lead-time gates + one new secret + deploy + pilot link + first-live-run verification of BOTH GA4 and GSC).

## Purpose
11-01/11-02/11-03 built the GA4+GSC vertical LOCAL with ZERO prod contact — nothing is migrated, secreted, committed,
or deployed. This is the single Phase-11 operator gate batch (the shared activation 11-02 + 11-03 both deferred to),
mirroring the proven Phase-10 10-03 runbook. It is the last step before Phase 12 (the PSG report) can consume all four
live sources.

## Output
- `.paul/phases/11-ga4-gsc/11-04-GATE-BATCH.md` — the ordered prod-activation runbook (authored; ALREADY written this plan).
- A `feature/11-ga4-gsc` branch + ONE local Phase-11 commit of the 11-01/11-02/11-03 trees (Task 2, NO push — the
  operator pushes/deploys/merges as Stage C/E of the runbook).
- LIVE on prod after the operator executes: GA4 "Website traffic" + GSC "Search performance" reading real numbers for a
  pilot shop. Honest partial allowed: GA4 live + GSC activation-pending if no verified GSC site exists yet (Stage 0.3).
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## The runbook this plan authors (deliverable)
@.paul/phases/11-ga4-gsc/11-04-GATE-BATCH.md

## What it activates (all LOCAL + gate-checked, ZERO prod contact)
@.paul/phases/11-ga4-gsc/11-01-SUMMARY.md   # 2 migrations (the prod-pending tables) + the OAuth link surface
@.paul/phases/11-ga4-gsc/11-02-SUMMARY.md   # GA4 ingest + cron + panel
@.paul/phases/11-ga4-gsc/11-03-SUMMARY.md   # GSC ingest + cron + panel

## Research (research-first gate — satisfied)
@.paul/phases/11-ga4-gsc/RESEARCH.md
# The live-verification items (consent publish for sensitive scopes, the gax authClient runtime smoke #1, the GSC
# siteUrl url-encoding #4 — RESOLVED in code this plan, and the GSC data-lag/max-date #3) are exactly the gate-batch
# pass-gates. No NEW external surface — this is the LIVE execution of the already-researched GA4+GSC contracts.

## The proven template
@.paul/phases/10-google-ads/10-03-GATE-BATCH.md   # the Phase-9+10 runbook this mirrors (executed live 2026-06-09)
@.paul/phases/06-rbac-rls-spine/PROTOCOL-migration-safety.md   # the per-migration advisor baseline→apply→diff→abort gate
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| Research-first / per-plan research check | required | Before authoring (this plan) | ✓ — RESEARCH.md (ultracode `wf_b732175b-025`) + the 11-01/02/03 summaries cover the live GA4+GSC contracts, consent, quotas/lag; the migration gate is the 06-01 PROTOCOL. No new external surface. |

**BLOCKING:** Satisfied. This is a live-activation runbook over already-researched + already-built code.
</skills>

<acceptance_criteria>

## AC-1: The runbook is authored, ordered, and executable
```gherkin
Given the Phase-11 build is local + uncommitted (2 prod-pending migrations, 1 new secret, nothing deployed)
When 11-04-GATE-BATCH.md is authored
Then it pins the verified deploy environment (repo psg-hub.git, toplevel /dev/psg/internal/psg-hub, .vercel
     rootDirectory apps/psg-hub/, the above-repo .vercel HAZARD, the prod DB, the migration PROTOCOL)
And it sequences Stage 0 lead-time blockers (consent for the 2 new SENSITIVE scopes with an EMPIRICAL >7-day-token
     pass-gate not an ETA; GA4 Admin+Data + GSC API enablement; a verified-pilot-GSC-site prerequisite) → Stage A
     (the TWO 11-01 migrations under PROTOCOL, each with its expected advisor diff + ABORT condition) → Stage B (the
     one new GOOGLE_ANALYTICS_OAUTH_REDIRECT_URI secret + verify the 5 already-set, NO dev token) → Stage C
     (branch feature/11-ga4-gsc + commit + deploy from the repo toplevel) → Stage D (pilot link + trigger both crons +
     verify REAL non-zero GA4 and GSC numbers, with the #1/#3/#4 probes as diagnosis paths) → Stage E (close, incl.
     the honest GA4-live / GSC-pending partial)
And every stage carries a checkbox pass-gate phrased as REAL NUMBERS / observed state, never "cron returned 200".
```

## AC-2: The GSC siteUrl double-encode defect is fixed in code before the runbook ships
```gherkin
Given gsc-metrics.ts originally pre-encoded siteUrl with encodeURIComponent
When the activation prep verified googleapis@173 against node_modules
Then it confirmed the client builds the path via an RFC-6570 url-template (/sites/{siteUrl}/searchAnalytics/query,
     expanded in googleapis-common/apirequest.js) that ALREADY percent-encodes the path param
And the manual encodeURIComponent was REMOVED (it would double-encode `:`/`/` → sc-domain%253A... → 404/403),
     siteUrl is now passed RAW, tsc clean + gsc-metrics tests still green
And the runbook records the live GSC 404/403 path as a double-encode-regression diagnosis, not a console toggle.
```

## AC-3: The Phase-11 trees are committed locally on a feature branch (NO push)
```gherkin
Given the working tree holds the uncommitted 11-01/11-02/11-03 code + plan/summary docs on `main`
When Task 2 runs
Then it creates `feature/11-ga4-gsc` off main (never commits Phase-11 work straight to the default branch) and lands
     ONE Phase-11 commit (feat(11-ga4-gsc): ...) of the google-oauth lib + the analytics/cron routes + the 2 migrations
     + page.tsx panels + types + vercel.json + e2e + the .paul/phases/11-ga4-gsc/* docs
And it does NOT push and does NOT deploy and does NOT touch prod — push/merge/deploy are the operator's Stage C/E.
```

## AC-4: Operator executes the runbook; the loop closes on REAL NUMBERS
```gherkin
Given the authored runbook + the local commit
When the operator works 11-04-GATE-BATCH.md against prod (checkpoint:human-action)
Then the 2 migrations apply with clean advisor diffs (google_oauth_accounts: RLS + 1 SELECT policy;
     google_oauth_pending_states: RLS on / 0 policies = one rls_enabled_no_policy INFO), the secret is set, the deploy
     is Ready with ƒ ga4-sync + ƒ gsc-sync, a pilot links one Google account (2 rows / 1 token), both crons run, and
     /dashboard/analytics shows REAL non-zero GA4 (sessions/users/key_events) AND GSC (clicks/impressions/position)
And Phase 11 closes LIVE — OR, if no verified GSC site exists yet, GA4 closes live + GSC = activation-pending (honest
     partial, not a defect), the GSC cron deployed and 200-ing synced:0 until a site is linked.
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Author the 11-04-GATE-BATCH.md runbook (DONE this plan)</name>
  <files>.paul/phases/11-ga4-gsc/11-04-GATE-BATCH.md</files>
  <action>
    Author the ordered operator runbook (ALREADY written): pinned verified environment (re-confirmed via git remote -v
    + cat .vercel/project.json this session); Stage 0 lead-time blockers (consent for analytics.readonly +
    webmasters.readonly with an EMPIRICAL >7-day token pass-gate; GA4 Admin/Data + GSC API enablement; the
    verified-pilot-GSC-site prerequisite with the honest GA4-live/GSC-pending partial); Stage A the TWO 11-01 migrations
    under PROTOCOL with per-migration expected-diff + ABORT; Stage B the one new GOOGLE_ANALYTICS_OAUTH_REDIRECT_URI
    secret (+ verify the 5 already-set, NO dev token); Stage C branch + commit + deploy from the repo toplevel (with the
    above-repo .vercel HAZARD called out); Stage D pilot link + both crons + REAL-NUMBER GA4+GSC verification with the
    #1 authClient / #3 lag / #4 siteUrl-encoding probes as diagnosis paths; Stage E close + the failure→fix table.
  </action>
  <verify>11-04-GATE-BATCH.md exists with all 6 stages, checkbox pass-gates phrased as real-numbers/observed-state, the verified-environment table, and the failure→fix reference.</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the GSC siteUrl double-encode (DONE) + branch feature/11-ga4-gsc + ONE local Phase-11 commit (NO push)</name>
  <files>src/lib/google-oauth/gsc-metrics.ts, "(git) feature/11-ga4-gsc"</files>
  <action>
    (a) **DONE this plan** — removed the `encodeURIComponent(siteUrl)` from gsc-metrics.ts defaultQuery (googleapis@173
    percent-encodes the {siteUrl} path param itself via RFC-6570 url-template — verified against
    googleapis-common/apirequest.js; the manual encode double-encoded). siteUrl now passed RAW; tsc clean, gsc-metrics
    6/6 green. AC-2.
    (b) At APPLY: `git checkout -b feature/11-ga4-gsc` off `main` (the Phase-11 work is uncommitted on the default
    branch — branch first), then ONE commit `feat(11-ga4-gsc): Phase 11 — GA4 + GSC ingest (11-01 + 11-02 + 11-03)`
    staging the full Phase-11 surface: src/lib/google-oauth/* + __tests__, src/app/api/analytics/google/* +
    src/app/api/cron/{ga4,gsc}-sync/*, supabase/migrations/20260609183451/52, src/app/dashboard/analytics/* (page +
    link button), src/lib/analytics/types.ts, next.config.ts, vercel.json, vitest.setup.ts, e2e/*, package.json +
    pnpm-lock.yaml (the 11-01 deps), and .paul/phases/11-ga4-gsc/* (plans + summaries + this gate batch).
    NO push, NO deploy, NO prod contact — those are the operator's Stage C/E.
  </action>
  <verify>`git rev-parse --abbrev-ref HEAD` = feature/11-ga4-gsc; `git log --oneline -1` is the Phase-11 commit; `git status` clean; `git branch -r` shows NO new pushed branch (local only). tsc + gsc-metrics tests green (AC-2 already verified).</verify>
  <done>AC-2 + AC-3 satisfied.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Execute 11-04-GATE-BATCH.md against prod — the Phase-11 GA4+GSC activation.</action>
  <instructions>
    Claude has authored the runbook and landed the local Phase-11 commit on feature/11-ga4-gsc (nothing pushed, nothing
    on prod). You execute `.paul/phases/11-ga4-gsc/11-04-GATE-BATCH.md` end to end:
    - Stage 0 FIRST (consent for the 2 new sensitive scopes — empirical >7-day token check; enable GA4 Admin/Data + GSC
      APIs; confirm a pilot shop has a GA4 property AND a verified GSC site — if no GSC site, GA4 closes live + GSC
      stays activation-pending).
    - Stage A — apply the TWO 11-01 migrations under PROTOCOL (advisor baseline → apply ONE → diff → ABORT on any
      unintended diff; expected: google_oauth_accounts RLS+1 SELECT policy, google_oauth_pending_states RLS/0-policy).
    - Stage B — `vercel env add GOOGLE_ANALYTICS_OAUTH_REDIRECT_URI production` (verify the 5 already-set; NO dev token).
    - Stage C — push feature/11-ga4-gsc and `vercel --prod` from /dev/psg/internal/psg-hub (confirm .vercel rootDirectory
      apps/psg-hub/ first; do NOT deploy from the above-repo .vercel).
    - Stage D — link a pilot Google account, trigger both crons with CRON_SECRET, and READ THE LIVE SURFACE.
    Claude can drive any CLI step you delegate (migrations via MCP, `vercel env`, deploy) when you authorize it; the
    consent screen, API enablement, GSC site verification, and the OAuth click-through are operator-only.
  </instructions>
  <verification>
    Loop closes on REAL NUMBERS, not cron-200:
    - GA4 "Website traffic" shows non-zero sessions/users/key_events for the pilot property (also the gax authClient live smoke).
    - GSC "Search performance" shows non-zero clicks/impressions/position for the pilot site (RAW siteUrl returns rows, not 404).
    - OR the honest partial: GA4 live + GSC activation-pending (no verified site yet) — recorded, not a defect.
    Then Phase 11 ✅ COMPLETE + LIVE → /paul:unify 11-04 → transition → /paul:plan Phase 12.
  </verification>
  <resume-signal>Report the live GA4 + GSC numbers (or the GA4-live/GSC-pending partial), or describe any stage that aborted.</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- The Phase-11 application code (11-01/02/03) beyond the one already-applied gsc-metrics siteUrl-encode fix — this plan
  is activation, not a rewrite. No new features, no scope add.
- The already-live `20260609000000_google_ads_oauth_pending` migration + all Phase-9/10 prod state — this batch applies
  ONLY the two `20260609183451/52` 11-01 tables.
- The Phase-10 secrets — reuse GOOGLE_OAUTH_CLIENT_ID/SECRET + ADS_STATE_SECRET + ADS_ENCRYPTION_KEY + CRON_SECRET; add
  ONLY GOOGLE_ANALYTICS_OAUTH_REDIRECT_URI.

## SCOPE LIMITS
- Claude does NOT push, deploy, apply a prod migration, set a prod secret, or run the OAuth flow autonomously — every
  prod-touching action is operator-gated (checkpoint:human-action), executed by or explicitly authorized by the operator.
- NO new code feature. The only code change in this plan is the gsc-metrics siteUrl-encode fix (a latent defect caught
  in activation prep), already applied + test-green.
- Multi-property/multi-site per shop stays deferred (the snapshot key is property/site-less — inherited 11-02/11-03 decision).
</boundaries>

<verification>
Before declaring 11-04 complete (at UNIFY, after the operator runs it):
- [ ] 11-04-GATE-BATCH.md authored with all 6 stages + real-number pass-gates (AC-1)
- [ ] gsc-metrics siteUrl encode fix applied; tsc + gsc tests green (AC-2)
- [ ] feature/11-ga4-gsc branch + one local Phase-11 commit, NOT pushed (AC-3)
- [ ] Operator executed: 2 migrations clean-diffed, secret set, deploy Ready, pilot linked, BOTH crons 200 (AC-4)
- [ ] REAL NUMBERS: GA4 + GSC non-zero on the live surface (or the recorded GA4-live/GSC-pending partial)
- [ ] Phase 11 LIVE → ready for the transition to Phase 12
</verification>

<success_criteria>
- The Phase-11 GA4+GSC vertical is LIVE on prod with real numbers for a pilot shop (or GA4 live + GSC activation-pending,
  honestly recorded), the two 11-01 tables migrated under PROTOCOL with clean advisor diffs, and the single Phase-11
  commit landed — closing the v0.3 analytics data foundation that Phase 12 (the PSG report) consumes.
- No blind activation: every prod step is a checkbox pass-gate verified on observed state, and the one latent code
  defect (GSC siteUrl double-encode) was caught + fixed before the runbook shipped.
</success_criteria>

<output>
After the operator executes and reports real numbers, create `.paul/phases/11-ga4-gsc/11-04-SUMMARY.md`. This is the
LAST Phase-11 plan — UNIFY fires the Phase-11 + (Phase-11-is-3rd-of-4-in-v0.3, so NOT milestone) phase transition →
PROJECT.md + ROADMAP.md evolve (Phase 11 → Complete + LIVE) → /paul:plan Phase 12.
</output>
