---
phase: 14-reviews-sentiment
plan: 04
type: execute
wave: 1
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - .paul/phases/14-reviews-sentiment/14-04-GATE-BATCH.md            # the operator runbook (authored Task 1, this plan)
  - "(prod) gylkkzmcmbdftxieyabw — 3 Phase-14 migrations applied under PROTOCOL (Task 2, Stage A)"
  - "(prod) psg-hub Vercel — vercel --prod deploying the committed gbp-reviews-sync cron (Task 2, Stage C)"
autonomous: false   # Task 2 is checkpoint:human-action — the operator works the runbook against prod; Claude drives delegated CLI (migrations via Supabase MCP, vercel --prod) when authorized. The loop closes on observed prod state, not local gates.
---

<objective>
## Goal
Activate the Phase-14 reviews + sentiment build on prod and close Phase 14. Operator routing =
"partial now, pending close" (the 13-04 precedent): apply the 3 Phase-14 migrations under PROTOCOL +
`vercel --prod` (the gate-INDEPENDENT prod work) NOW so the committed `gbp-reviews-sync` cron goes
live (200/synced:0), then close 14-04 ACTIVATION-PENDING on the shared Gate A/B (re-checked — they
may have cleared since 13-04 filed them), the live v4 reviews LIST smoke, and the empirical 7-day
token pass-gate. The gateway-Haiku sentiment smoke is NOT Google-gated and can de-risk early against a
seeded row. This plan authors the runbook (`14-04-GATE-BATCH.md`, DONE this plan); the operator
executes it (Claude drives delegated CLI).

## Purpose
14-01..14-03 built the full reviews vertical (per-review v4 ingest → reply-publish plumbing → LLM
sentiment) LOCAL + gate-verified, committed to main (`e86b28c`) — but nothing is on prod: 3 migrations
unapplied, the 10th cron undeployed (main deploy is OFF), no shop linked under `business.manage`. This
is the single Phase-14 operator gate batch and a DELTA over 13-04 (which already put the Phase-13
migrations + 9 crons live). It shares Phase 13's pending tail — Gate A (GBP API 300 QPM), Gate B
(`business.manage` verification), the Wallace re-consent, the 7-day pass-gate — so per 13-RESEARCH it
most likely closes activation-pending until those external gates clear.

## Output
- `.paul/phases/14-reviews-sentiment/14-04-GATE-BATCH.md` — the ordered prod-activation runbook,
  authored this plan as a DELTA over 13-04 (Stage 0 Gate A/B re-check + the new legacy-GMB-API QPM line
  → Stage A the 3 Phase-14 migrations under PROTOCOL → Stage B verify secrets, NO new → Stage C
  `vercel --prod` the gbp-reviews-sync cron, 10 crons, reply cron absent → Stage D the two independent
  smoke axes [D1 v4 LIST Google-gated, D2 gateway-Haiku sentiment NOT Google-gated, D3 the 7-day
  pass-gate] → Stage E close incl. the activation-pending partial + the 14-02b/14-03b out-of-scope
  follow-ups).
- Prod state after the operator runs Stage A-C: the 3 migrations applied (clean advisor diff) + the
  gbp-reviews-sync cron LIVE (10 crons, 200/synced:0). Phase 14 closes activation-pending on Gate A/B
  (the honest partial) OR LIVE on real Wallace reviews + sentiment if the gates have already cleared.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## The runbook this plan authors (deliverable)
@.paul/phases/14-reviews-sentiment/14-04-GATE-BATCH.md

## Research (research-first gate — satisfied; no new external surface)
@.paul/phases/14-reviews-sentiment/14-RESEARCH.md
# v4 reviews.list READ + updateReply WRITE contracts, the policy/consent gap, the LLM-sentiment design.
@.paul/phases/13-gbp-presence/13-RESEARCH.md
# Gate A (0→300 QPM, ~14-day review, per-API quota trap incl. the legacy GMB API line) + Gate B
# (business.manage verification, In-Production → no Testing escape) + the empirical >7-day token pass-gate.

## What it activates (all LOCAL + gate-checked, committed to main e86b28c)
@.paul/phases/14-reviews-sentiment/14-01-SUMMARY.md   # per-review v4 ingest; migration 20260616163539 (review_items widen+UNIQUE, analytics_sync_runs +gbp_reviews); the 10th cron gbp-reviews-sync; the LIST live-smoke checklist
@.paul/phases/14-reviews-sentiment/14-02-SUMMARY.md   # reply-publish plumbing; migration 20260616231817 (review_responses publish-state); the UNSCHEDULED reply cron (OUT of vercel.json); the consent/legal gate (14-02b)
@.paul/phases/14-reviews-sentiment/14-03-SUMMARY.md   # LLM sentiment; migration 20260617120000 (review_sentiment table+RLS); classify-on-ingest into gbp-reviews-sync; the gateway-Haiku live-smoke

## The proven templates (this plan is a DELTA over 13-04)
@.paul/phases/13-gbp-presence/13-04-GATE-BATCH.md     # the immediately-prior gate batch; shared deploy env + PROTOCOL + Gate A/B mechanics + the above-repo ../.vercel hazard live here
@.paul/phases/13-gbp-presence/13-04-PLAN.md           # the gate-batch PLAN shape this mirrors (Task 1 author / Task 2 checkpoint:human-action)
@.paul/phases/06-rbac-rls-spine/PROTOCOL-migration-safety.md   # the per-migration advisor baseline→apply→diff→ABORT gate
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| Research-first / per-plan research check | required | Before authoring (this plan) | ✓ — `14-RESEARCH.md` (ultracode `wf_4ac2ec22-54d`) covers the v4 reviews READ/WRITE contracts + the LLM-sentiment design; `13-RESEARCH.md` covers Gate A/B + the empirical pass-gate; the migration gate is the 06-01 PROTOCOL. No new external surface — live activation of already-researched + already-built code. |

**BLOCKING:** Satisfied. This is a live-activation runbook over already-researched, already-built,
already-committed code (the 13-04 precedent).
</skills>

<acceptance_criteria>

## AC-1: The runbook is authored as a delta over 13-04, ordered, and executable
```gherkin
Given the Phase-14 build is committed to main (e86b28c) but ZERO prod (3 migrations unapplied, the 10th cron undeployed, no business.manage link)
When 14-04-GATE-BATCH.md is authored
Then it references 13-04 for the shared deploy environment + PROTOCOL + the ../.vercel hazard + the Gate A/B mechanics, and spells out ONLY the Phase-14 deltas
And it sequences Stage 0 (RE-CHECK Gate A — incl. the Phase-14-specific legacy Google My Business API 300 QPM line per-review reviews.list hits — and Gate B) → Stage A (the 3 Phase-14 migrations under PROTOCOL, each with the auto-named-constraint verify + the proof-insert/clean + ABORT-on-unexpected-diff) → Stage B (verify the 4 Phase-11 creds + AI_GATEWAY_API_KEY, NO new secret) → Stage C (vercel --prod from the toplevel, 10 crons, gbp-reviews-sync present, gbp-reviews-reply ABSENT) → Stage D (D1 v4 LIST Google-gated, D2 gateway-Haiku sentiment NOT Google-gated, D3 the 7-day pass-gate) → Stage E (close incl. the activation-pending partial)
And it names the OUT-of-scope follow-ups (14-02b reply-publish live activation behind legal sign-off; 14-03b sentiment surface) so they are not silently folded into the batch
```

## AC-2: The 3 Phase-14 migrations apply to prod under PROTOCOL with a clean advisor diff (RUNNABLE NOW)
```gherkin
Given the 3 committed-but-unapplied Phase-14 migrations (20260616163539, 20260616231817, 20260617120000)
When the operator authorizes Stage A (Claude drives via the Supabase MCP)
Then each applies to gylkkzmcmbdftxieyabw under PROTOCOL (baseline → apply ONE → diff → ABORT on any unintended ERROR/WARN): migrations 1+2 (CHECK swap + additive columns) show ZERO new ERROR/WARN; migration 3 (a NEW review_sentiment table WITH its 4 RLS policies in the same migration) shows no new ERROR/WARN and NO rls_enabled_no_policy — and because its policies mirror the live review_items policies, the bar is matching review_items' advisor footprint (a baselined INFO-level perf lint like auth_rls_initplan is NOT a new finding), not literally zero-delta
And the auto-named analytics_sync_runs_source_check name is confirmed (the widen drops by LIVE name)
And the end state is proven WITHOUT FK-blocked inserts: analytics_sync_runs accepts 'gbp_reviews' via a live insert (bogus rejected 23514; proof rows cleaned — the 13-04 pattern); review_items / review_responses / review_sentiment are confirmed STRUCTURALLY via \d + pg_constraint (a bare insert is FK-blocked on prod at synced:0 → 23503, and their CHECK behavior was already proven locally) — review_items carries external_review_id + updated_at + UNIQUE(shop_id, external_review_id); review_responses carries the 5 publish cols + the publish_status CHECK; review_sentiment carries the polarity CHECK + UNIQUE(review_item_id) + RLS on + the 4 shop_id-clamped policies
```

## AC-3: The committed gbp-reviews-sync cron deploys LIVE via vercel --prod, no new secret, reply cron absent (RUNNABLE NOW)
```gherkin
Given the committed vercel.json carries 10 crons incl. gbp-reviews-sync (0 8) and main does NOT auto-deploy
When the operator authorizes Stage C
Then `vercel --prod` runs from the repo toplevel (the repo .vercel → psg-hub confirmed; NOT the above-repo ../.vercel), the deploy is READY listing ƒ /api/cron/gbp-reviews-sync, and vercel.json shows 10 crons with gbp-reviews-sync after gbp-sync and the gbp-reviews-reply cron ABSENT (legal-gated)
And Stage B confirms the 4 Phase-11 creds + AI_GATEWAY_API_KEY present with NO new secret set
And the cron answers 401 unauth and, with Bearer $CRON_SECRET, 200 with synced:0 + sentiment.classified:0 (the expected partial — no business.manage-linked shop yet; a green cron is NOT proof of classification, which is asserted in Stage D2)
```

## AC-4: Live activation OR an honest activation-pending close — sentiment asserted as ROWS, not cron 200
```gherkin
Given Stage A-C done (migrations live, cron deployed) and the two Google gates re-checked
When Gate A (300 QPM incl. the legacy GMB API line) AND Gate B (business.manage) are evaluated
Then IF both cleared: the operator re-consents Wallace under business.manage, D1 runs the v4 LIST smoke to REAL review_items rows (inserted>0; StarRating values, non-verified-location shape, idempotent dedup, QPM headroom, token-authorizes-read), D2 asserts a review_sentiment ROW with a valid polarity + confidence∈[0,1] (NOT a cron 200 — classify is contained-on-failure), and the empirical 7-day token pass-gate clears → Phase 14 closes LIVE
And IF either gate is NOT yet cleared (the expected outcome): 14-04 closes ACTIVATION-PENDING — migrations live + cron 200/synced:0 recorded honestly (the Phase-9/13 precedent), the surfaces degrade gracefully with no row, and the activation lights up when the gates clear with no further code change; the D2 sentiment smoke (NOT Google-gated) SHOULD still run against a seeded row to de-risk the build-blind Haiku parser independently
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Author the 14-04-GATE-BATCH.md runbook (DONE this plan)</name>
  <files>.paul/phases/14-reviews-sentiment/14-04-GATE-BATCH.md</files>
  <action>
    Author the ordered operator runbook (ALREADY written this plan) as a DELTA over 13-04: the
    "what 14-04 adds" table (3 migrations + 1 cron + 2 smoke axes + NO new secret); Stage 0 the Gate
    A/B RE-CHECK with the Phase-14-specific legacy Google My Business API 300 QPM line (per-review
    reviews.list); Stage A the 3 Phase-14 migrations under PROTOCOL with the auto-named
    analytics_sync_runs_source_check verify + the analytics_sync_runs insert-proof + the 3 FK-child
    tables structurally confirmed (\d + pg_constraint; their inserts are FK-blocked on prod at synced:0)
    + the expected advisor diff (review_items-footprint match for the new review_sentiment table; NO
    rls_enabled_no_policy) + ABORT; Stage B verify the 4 Phase-11 creds + AI_GATEWAY_API_KEY (NO new secret); Stage C
    `vercel --prod` from the repo toplevel (above-repo ../.vercel HAZARD referenced to 13-04) → 10
    crons + ƒ gbp-reviews-sync + the gbp-reviews-reply cron ABSENT + 200/synced:0; Stage D the two
    independent smoke axes (D1 v4 LIST Google-gated to REAL review_items rows; D2 gateway-Haiku
    sentiment NOT Google-gated, asserting review_sentiment ROWS not cron 200, de-riskable against a
    seeded row; D3 the empirical 7-day token pass-gate); Stage E close incl. the activation-pending
    honest partial; the OUT-of-scope 14-02b + 14-03b named follow-ups.
  </action>
  <verify>14-04-GATE-BATCH.md exists with all stages 0-E, the delta framing (references 13-04, spells out only the new), checkbox pass-gates phrased as observed prod state, the sentiment-asserts-rows-not-200 instruction, and the out-of-scope follow-ups.</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Execute 14-04-GATE-BATCH.md against prod — the Phase-14 reviews + sentiment activation (partial now, pending close).</action>
  <instructions>
    Claude has authored the runbook (nothing on prod; the Phase-14 build is committed to main e86b28c).
    You execute `.paul/phases/14-reviews-sentiment/14-04-GATE-BATCH.md`. Per the chosen routing, do the
    gate-INDEPENDENT prod work NOW and close activation-pending on the Google gates:
    - Stage 0 — RE-CHECK Gate A (the GCP Quotas page — incl. the legacy Google My Business API line at
      300 QPM for per-review reviews.list) + Gate B (business.manage verification). 13-04 filed these
      ~1 day ago; if both have cleared, 14-04 closes LIVE. They do NOT block Stage A-C.
    - Stage A (NOW) — apply the 3 Phase-14 migrations under PROTOCOL (advisor baseline → apply ONE →
      diff → ABORT on any unintended ERROR/WARN; verify the auto-named analytics_sync_runs_source_check;
      insert-proof analytics_sync_runs then clean + structurally confirm review_items/review_responses/
      review_sentiment via \d + pg_constraint [their inserts are FK-blocked on prod at synced:0]). Claude
      drives via the Supabase MCP when you authorize.
    - Stage B (NOW) — confirm the 4 Phase-11 creds + AI_GATEWAY_API_KEY present (NO new secret).
    - Stage C (NOW) — `vercel --prod` from /Users/schoolcraft_mbpro/dev/psg/internal/psg-hub (confirm the
      repo .vercel → psg-hub first; do NOT deploy from the above-repo ../.vercel). Verify 10 crons +
      ƒ gbp-reviews-sync + the gbp-reviews-reply cron ABSENT + 200/synced:0. Claude drives the deploy when you authorize.
    - Stage D (PENDING) — D1 (Google-gated) only once Gate A + Gate B clear + Wallace re-consents: the
      v4 LIST smoke to REAL review_items rows. D2 (NOT Google-gated) — run the gateway-Haiku sentiment
      smoke against a seeded review_items row to assert a review_sentiment ROW with a valid polarity
      (de-risks the build-blind Haiku parser independently of Google). D3 — the empirical 7-day token pass-gate.
    Claude drives any delegated CLI/MCP step (migrations, vercel --prod, the $CRON_SECRET curl triggers)
    when you authorize it; the Wallace OAuth click-through + the Gate A/B status are operator-only.
    Do NOT add the gbp-reviews-reply cron to vercel.json, do NOT build the publish UI, do NOT run any v4
    WRITE smoke — that is the 14-02b follow-up behind legal sign-off.
  </instructions>
  <verification>
    Loop closes on OBSERVED prod state, not local gates:
    - Stage A: 3 migrations applied, advisor diff per the AC-2 expectation (review_items-footprint match
      for review_sentiment; no rls_enabled_no_policy), the analytics_sync_runs_source_check name confirmed,
      the analytics_sync_runs insert-proof accepted/bogus-rejected/cleaned + the 3 FK-child tables
      structurally confirmed via \d + pg_constraint.
    - Stage C: deploy READY, 10 crons, ƒ gbp-reviews-sync, gbp-reviews-reply ABSENT, 200/synced:0 with $CRON_SECRET.
    - Stage D2 (recommended even when pending): a review_sentiment ROW with a valid polarity from a real
      gateway-Haiku round-trip (asserted as a row, NOT cron 200).
    - THEN either: Stage D1 live (Wallace REAL review_items rows + the 7-day pass-gate) → Phase 14 LIVE;
      OR the honest activation-pending close (Gate A/B not yet cleared) — migrations live + cron 200/synced:0 recorded.
    Then /paul:unify 14-04 → the Phase-14 transition (last plan) → milestone v0.3.5 ready for /paul:complete-milestone.
  </verification>
  <resume-signal>Report the Stage A advisor diffs + the Stage C deploy state + the Gate A/B status + (if run) the Stage D2 sentiment row. State whether 14-04 closes LIVE or activation-pending.</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- The Phase-14 application code (14-01..14-03) — it is built, tested, committed (e86b28c), on main.
  This plan is ACTIVATION, not a rewrite. No new feature, no scope add. (Any defect surfaced at the
  Stage D live smoke is a DEVIATION to fix-then-record, the 12-04/13-04 precedent — not pre-planned scope.)
- All Phase-9/10/11/12/13 prod state — this batch applies ONLY the 3 Phase-14 migrations (20260616163539,
  20260616231817, 20260617120000) and deploys the already-committed gbp-reviews-sync cron. No other
  migration, no secret change.
- `vercel.json` — do NOT add the `gbp-reviews-reply` cron (it is correctly absent; live publish is the
  legal-gated 14-02b follow-up). The deploy ships the committed 10 crons unchanged.

## SCOPE LIMITS
- Claude does NOT run the Wallace OAuth flow or read the Gate A/B console status autonomously — those are
  operator-only. Claude DOES drive the delegated CLI/MCP (the 3 migrations under PROTOCOL, `vercel --prod`,
  the $CRON_SECRET curl triggers) when the operator authorizes each.
- Per the "partial now" routing: Stage A-C execute now (gate-independent); Stage D1 (the v4 LIST smoke) is
  the activation-pending tail that runs when Gate A + Gate B clear; Stage D2 (the sentiment smoke) is NOT
  Google-gated and should run against a seeded row regardless.
- OUT of scope (named follow-ups): 14-02b reply-publish LIVE activation (legal sign-off + consent schema +
  UI + the reply cron + v4 WRITE smokes); 14-03b sentiment surface (report block + dashboard panel + the
  human-review queue + a CI golden-set gate). Fleet batching (842 shops) stays a deferred follow-on (pilot = Wallace).
</boundaries>

<verification>
Before declaring 14-04 complete (at UNIFY, after the operator runs it):
- [ ] 14-04-GATE-BATCH.md authored as a delta over 13-04 with all stages 0-E + observed-state pass-gates + the out-of-scope follow-ups (AC-1)
- [ ] Stage A: 3 migrations applied under PROTOCOL, advisor diff per expectation (review_items-footprint match for review_sentiment; no rls_enabled_no_policy), constraint name confirmed, analytics_sync_runs insert-proof + the 3 FK-child tables structurally confirmed via \d + pg_constraint (AC-2)
- [ ] Stage B: 4 Phase-11 creds + AI_GATEWAY_API_KEY confirmed, NO new secret (AC-3)
- [ ] Stage C: vercel --prod READY, 10 crons, ƒ gbp-reviews-sync, gbp-reviews-reply ABSENT, 200/synced:0 (AC-3)
- [ ] Stage D2 sentiment smoke: a review_sentiment ROW with a valid polarity (asserted as a row, not cron 200) — run even when pending (AC-4)
- [ ] Stage D1 LIVE (Wallace real review_items rows + 7-day pass-gate) OR activation-pending recorded honestly (AC-4)
- [ ] Phase 14 closes → ready for the transition (PROJECT/ROADMAP evolve; Phase 14 Complete or Complete+activation-pending)
</verification>

<success_criteria>
- The 3 Phase-14 migrations are applied to prod under PROTOCOL with a clean advisor diff and the
  gbp-reviews-sync cron is LIVE (10 crons, 200/synced:0, the reply cron correctly absent), with the
  activation honestly closed: LIVE on real Wallace reviews + sentiment if Gate A + Gate B have cleared,
  else activation-pending on the two external Google gates + the empirical 7-day token pass-gate (the
  expected outcome per 13-RESEARCH).
- No blind activation: every prod step is a checkbox pass-gate on observed state, the migration trap
  (auto-named constraint) is verified not assumed, and the sentiment smoke asserts review_sentiment ROWS
  (never a green cron) before any "sentiment LIVE" claim.
</success_criteria>

<output>
After the operator executes and reports observed prod state, create
`.paul/phases/14-reviews-sentiment/14-04-SUMMARY.md`. This is the LAST Phase-14 plan — UNIFY fires the
Phase-14 transition (PROJECT.md + ROADMAP.md evolve: Phase 14 → Complete, or Complete + activation-pending)
→ milestone v0.3.5 ready for `/paul:complete-milestone` (after the 14-03b sentiment-surface / 14-02b
reply-activation sequencing decision).
</output>
