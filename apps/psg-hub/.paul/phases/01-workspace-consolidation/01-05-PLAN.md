---
phase: 01-workspace-consolidation
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/package.json (NEW from BSM, name renamed)
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/next.config.* (from BSM)
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/tsconfig.json (from BSM)
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/tailwind.config.* (from BSM)
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/postcss.config.* (from BSM)
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/vitest.config.* (from BSM if exists)
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/middleware.* (from BSM if exists)
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/app/ (full tree from BSM)
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/components/ (full tree from BSM)
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/lib/ (full tree from BSM)
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/public/ (full tree from BSM)
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.env.example (from BSM)
  - /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/references/bsm/* (absorbed BSM PAUL)
  - /Users/schoolcraft_mbpro/apps/psg/pnpm-lock.yaml (NEW; generated by pnpm install at workspace root)
relocated_paths:
  - /Users/schoolcraft_mbpro/apps/projects/bsm/dashboard/ → /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/ (merge with graduation skeleton; psg-hub .paul/ + .git + README.md preserved)
  - /Users/schoolcraft_mbpro/apps/projects/bsm/.paul/ → copied to /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/references/bsm/
autonomous: false
---

<objective>
## Goal

Relocate BSM dashboard codebase (`~/apps/projects/bsm/dashboard/`) into the psg-hub directory as the anchor app (D3). Preserve psg-hub's graduation skeleton (existing `.git`, fresh `.paul/`, v7 README.md). Absorb BSM PAUL state into `.paul/references/bsm/` so BSM's Phase 1–5 history stays accessible without becoming the active PAUL state (D8 — BSM PAUL preserved as foundation, psg-hub milestones continue at v0.1). Generate the first workspace lockfile via `pnpm install` at the workspace root.

## Purpose

01-01 stood up the monorepo container. This plan lands the first workspace member — the BSM anchor — and gives the workspace a real lockfile. After 01-05 the workspace has one real app, one real pnpm-lock.yaml, and Wave 2's remaining plans (01-06 siblings → packages, 01-07 apps/ads → Python worker) can pile on. BSM siblings (studio, integrations, onboarding, preview, shops) stay at `~/apps/projects/bsm/` until 01-06.

## Output

- `apps/psg/apps/psg-hub/` contains the BSM dashboard codebase merged with the graduation skeleton
- `apps/psg/apps/psg-hub/package.json` has `name: "psg-hub"` (renamed from BSM's name) + scripts + deps from BSM
- `apps/psg/apps/psg-hub/.paul/{PROJECT.md, ROADMAP.md, STATE.md, paul.json, phases/}` UNCHANGED (psg-hub's fresh PAUL stays authoritative)
- `apps/psg/apps/psg-hub/.paul/references/bsm/` contains BSM PAUL state (PROJECT.md, ROADMAP.md, STATE.md, phases/01–05/) as historical foundation
- `apps/psg/apps/psg-hub/README.md` UNCHANGED (v7 graduation README stays); BSM's original README copied to `.paul/references/bsm/ORIGINAL-README.md`
- `apps/psg/pnpm-lock.yaml` generated at workspace root
- `~/apps/projects/bsm/dashboard/` no longer exists; `~/apps/projects/bsm/{studio, integrations, onboarding, preview, shops, .paul, ...}` remain for 01-06
- `pnpm dev --filter=psg-hub` and `pnpm build --filter=psg-hub` both succeed
</objective>

<context>
@/Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/PROJECT.md
@/Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/ROADMAP.md
@/Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/STATE.md
@/Users/schoolcraft_mbpro/apps/psg/projects/psg-hub/PLANNING.md
@/Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/phases/01-workspace-consolidation/01-01-PLAN.md

## BSM source
@/Users/schoolcraft_mbpro/apps/projects/bsm/.paul/PROJECT.md
@/Users/schoolcraft_mbpro/apps/projects/bsm/.paul/ROADMAP.md
@/Users/schoolcraft_mbpro/apps/projects/bsm/.paul/STATE.md
@/Users/schoolcraft_mbpro/apps/projects/bsm/dashboard/package.json
@/Users/schoolcraft_mbpro/apps/projects/bsm/PLANNING.md
</context>

<acceptance_criteria>

## AC-1: Pre-scan documents merge conflicts before any write
```gherkin
Given BSM dashboard/ and psg-hub/ both contain files at top level
When pre-scan runs
Then it identifies any name collisions (e.g., README.md, .gitignore, .env.example)
And it identifies which BSM files would overwrite psg-hub graduation files
And it records the conflict resolution plan: psg-hub preserves .paul/, README.md, .git; BSM overrides everything else
```

## AC-2: BSM PAUL absorbed to references/bsm/
```gherkin
Given BSM PAUL state exists at ~/apps/projects/bsm/.paul/
When the absorption task runs
Then apps/psg-hub/.paul/references/bsm/ contains: PROJECT.md, ROADMAP.md, STATE.md, paul.json, phases/01-*/* through phases/05-*/*, handoffs/* if present
And the absorbed copy preserves original mtime
```

## AC-3: BSM dashboard contents merged into psg-hub
```gherkin
Given BSM dashboard pre-clean is complete (no node_modules / .next / .turbo / .vercel / .git)
When merge runs
Then apps/psg-hub/ contains BSM's app/, components/, lib/, public/, config files (next.config.*, tsconfig.json, tailwind.config.*, postcss.config.*, vitest.config.*), package.json, middleware.* if present, .env.example if present
And apps/psg-hub/.paul/ (top level) UNCHANGED — still has PROJECT/ROADMAP/STATE/paul.json/phases/ from graduation init
And apps/psg-hub/README.md UNCHANGED — still the v7 graduation README
And apps/psg-hub/.git UNCHANGED — still the fresh git init from graduation
And ~/apps/projects/bsm/dashboard/ returns ENOENT
```

## AC-4: package.json renamed
```gherkin
Given BSM's package.json has its own "name" field (likely "bsm-dashboard" or similar)
When the rename task runs
Then apps/psg-hub/package.json has "name": "psg-hub"
And the original BSM name is recorded in SUMMARY for cross-reference
And other package.json fields (scripts, dependencies, devDependencies, engines, packageManager) are preserved
```

## AC-5: Workspace install succeeds + dev/build smoke
```gherkin
Given the merge is complete and workspace-root configs from 01-01 are in place
When `pnpm install` runs from apps/psg/
Then a pnpm-lock.yaml file is generated at apps/psg/
And psg-hub is recognized as a workspace member (`pnpm -r list` shows psg-hub)
And `pnpm typecheck --filter=psg-hub` exits 0
And `pnpm build --filter=psg-hub` exits 0 (no test data, but build must succeed without missing env vars beyond .env.local)
```

## AC-6: BSM siblings untouched
```gherkin
Given 01-06 will relocate BSM siblings later
When 01-05 completes
Then ~/apps/projects/bsm/ still contains: studio/, integrations/, onboarding/, preview/, shops/, .paul/ (BSM's; copy went to references/), PLANNING.md, any top-level README/config
And only dashboard/ was removed
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Pre-scan BSM dashboard vs psg-hub graduation skeleton</name>
  <files>/Users/schoolcraft_mbpro/apps/psg/projects/psg-hub/.paul-bridge/01-05-pre-scan.md</files>
  <action>
Non-destructive scan:

```bash
SRC=/Users/schoolcraft_mbpro/apps/projects/bsm/dashboard
DST=/Users/schoolcraft_mbpro/apps/psg/apps/psg-hub

# Source top-level
ls -la "$SRC"
du -sh "$SRC"

# Destination top-level (graduation skeleton)
ls -la "$DST"

# Detect conflicts: files in DST that BSM dashboard also has
comm -12 <(ls "$SRC" | sort) <(ls "$DST" | sort)

# BSM .paul top-level (NOT inside dashboard/) — for references/bsm absorption
ls -la /Users/schoolcraft_mbpro/apps/projects/bsm/.paul

# Identify pre-clean targets in source
find "$SRC" -maxdepth 3 -type d \
  \( -name node_modules -o -name .next -o -name .turbo -o -name .vercel \
     -o -name dist -o -name build -o -name coverage \) 2>/dev/null

# Identify .git tree in source
ls -la "$SRC/.git" 2>/dev/null && echo "BSM dashboard has .git — will be dropped per D6"

# Package.json name field
grep '"name"' "$SRC/package.json" | head -1
```

Persist to `apps/psg/projects/psg-hub/.paul-bridge/01-05-pre-scan.md`:
- Source top-level + size
- Destination top-level (graduation skeleton state)
- Conflict list (file names in both)
- BSM .paul top-level entries
- Pre-clean targets + sizes
- Source `.git` confirmation
- Source `package.json` name field

Conflict resolution policy (record in scan):
- `.paul/` — psg-hub preserves; BSM dashboard's own `.paul` (if any inside `dashboard/`) is skipped
- `README.md` — psg-hub preserves; BSM's becomes `.paul/references/bsm/ORIGINAL-README.md`
- `.git` — psg-hub preserves; BSM source `.git` dropped before merge
- `.gitignore` — keep workspace-root from 01-01 + accept BSM's app-level one if present
- `.env.example` — accept BSM's
- `.env*` (with actual values) — DO NOT COPY (security boundary)
- All other files (app/, components/, lib/, public/, configs) — BSM overrides (psg-hub skeleton has none of these)

Avoid: any mutations.
  </action>
  <verify>
File exists at `.paul-bridge/01-05-pre-scan.md`. Conflict list visible. BSM name field captured.
  </verify>
  <done>AC-1 satisfied: merge surveyed before write.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
Pre-scan at `.paul-bridge/01-05-pre-scan.md` shows source contents, destination skeleton, conflict list, and the resolution policy (psg-hub preserves .paul/, README.md, .git; BSM source .git + .next + .vercel + node_modules pre-cleaned; .env files NOT copied).
  </what-built>
  <how-to-verify>
1. Open the pre-scan file
2. Confirm no surprising files in BSM dashboard (e.g., un-gitignored credentials, large binary blobs)
3. Confirm the conflict list matches expectation: only meta files (.gitignore, README.md, .paul, .git, .env.example)
4. Confirm BSM has no live customer data in dashboard/ (D57 says zero live customers; this is a sanity check)
5. Confirm operator is OK with dropping BSM source .git (D6 — no history preservation)
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with absorption + pre-clean + merge + install, or describe overrides (specific files to copy/skip, alternate name for package.json).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Absorb BSM PAUL into references/bsm/</name>
  <files>/Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/references/bsm/* (created)</files>
  <action>
```bash
mkdir -p /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/references/bsm
cp -pR /Users/schoolcraft_mbpro/apps/projects/bsm/.paul/* /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/references/bsm/
# Also surface top-level BSM project docs
cp -p /Users/schoolcraft_mbpro/apps/projects/bsm/PLANNING.md /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/references/bsm/ORIGINAL-PLANNING.md 2>/dev/null || true
cp -p /Users/schoolcraft_mbpro/apps/projects/bsm/README.md   /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/references/bsm/ORIGINAL-README.md 2>/dev/null || true
# If dashboard/README.md exists, surface it separately (developer-facing brief vs project-level BSM README)
cp -p /Users/schoolcraft_mbpro/apps/projects/bsm/dashboard/README.md /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/references/bsm/DASHBOARD-README.md 2>/dev/null || true
```

Avoid: copying BSM's `.git` (history not preserved per D6). Avoid: copying BSM's node_modules or .next.
  </action>
  <verify>
`ls /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/references/bsm/` shows PROJECT.md, ROADMAP.md, STATE.md (minimum), plus phases/. `grep -c "Phase 5\|Phase 4\|Phase 3" /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/.paul/references/bsm/ROADMAP.md` ≥ 3 (confirms BSM's 5-phase history present).
  </verify>
  <done>AC-2 satisfied: BSM PAUL absorbed.</done>
</task>

<task type="auto">
  <name>Task 3: Pre-clean BSM dashboard source</name>
  <files>(destructive cleanup inside ~/apps/projects/bsm/dashboard/ before merge)</files>
  <action>
```bash
SRC=/Users/schoolcraft_mbpro/apps/projects/bsm/dashboard
find "$SRC" -depth -type d \
  \( -name node_modules -o -name .next -o -name .turbo -o -name .vercel \
     -o -name dist -o -name build -o -name coverage \) \
  -exec rm -rf {} +
rm -rf "$SRC/.git"
find "$SRC" -type f -name .DS_Store -delete
# Remove any .env files containing real values (keep .env.example)
find "$SRC" -maxdepth 2 -type f -name ".env*" ! -name ".env.example" -delete
```

Record bytes freed + any .env files dropped in SUMMARY.

Avoid: deleting `src/`, `app/`, `components/`, `lib/`, `public/`, configs, package.json, .env.example. Avoid: traversing into sibling BSM directories (studio/, integrations/, etc.).
  </action>
  <verify>
`find /Users/schoolcraft_mbpro/apps/projects/bsm/dashboard -type d -name node_modules` returns no matches. `ls /Users/schoolcraft_mbpro/apps/projects/bsm/dashboard/.git 2>&1` returns ENOENT. `ls /Users/schoolcraft_mbpro/apps/projects/bsm/dashboard/.env 2>&1 || ls /Users/schoolcraft_mbpro/apps/projects/bsm/dashboard/.env.local 2>&1` returns ENOENT (or matches if scan said none existed).
  </verify>
  <done>Pre-clean complete; BSM dashboard ready to merge.</done>
</task>

<task type="auto">
  <name>Task 4: Merge BSM dashboard into psg-hub (preserve graduation skeleton)</name>
  <files>(many) BSM contents land at /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/</files>
  <action>
Selective merge — copy BSM dashboard contents into psg-hub, skipping items that would overwrite the graduation skeleton (`.paul/`, `.git/`, `README.md`).

```bash
SRC=/Users/schoolcraft_mbpro/apps/projects/bsm/dashboard
DST=/Users/schoolcraft_mbpro/apps/psg/apps/psg-hub

# rsync is the most predictable tool for selective merge.
rsync -a \
  --exclude='.paul' \
  --exclude='.git' \
  --exclude='README.md' \
  --exclude='node_modules' \
  --exclude='.next' \
  --exclude='.turbo' \
  --exclude='.vercel' \
  --exclude='coverage' \
  --exclude='.env' \
  --exclude='.env.local' \
  --exclude='.env.development' \
  --exclude='.env.production' \
  --exclude='.env.test' \
  --exclude='.DS_Store' \
  "$SRC/" "$DST/"

# After rsync confirms success, remove source dashboard/
rm -rf "$SRC"
```

If rsync is not available, use `cp -pR` per top-level entry with the same exclusion list applied manually.

Avoid: removing source until rsync confirmed all files landed. Avoid: copying .env files with secrets.
  </action>
  <verify>
`ls /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/` shows app/, components/, lib/, public/, package.json, next.config.*, tsconfig.json, tailwind.config.* (subset depending on BSM structure). `.paul/` top level remains with PROJECT.md/ROADMAP.md/STATE.md/paul.json (not overwritten). `README.md` byte-size matches v7 graduation README. `.git/` still exists (graduation init). `~/apps/projects/bsm/dashboard 2>&1` returns ENOENT. `~/apps/projects/bsm/studio` still exists (sibling untouched).
  </verify>
  <done>AC-3 + AC-6 satisfied: merge complete; siblings untouched.</done>
</task>

<task type="auto">
  <name>Task 5: Rename package.json name field</name>
  <files>/Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/package.json</files>
  <action>
Read current package.json. Change only the `name` field to `"psg-hub"` (or `"@psg/psg-hub"` if operator overrode in checkpoint). Preserve everything else (version, scripts, dependencies, devDependencies, engines, packageManager, optional fields).

Use jq for safe in-place edit:
```bash
PKG=/Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/package.json
ORIGINAL_NAME=$(jq -r '.name' "$PKG")
echo "ORIGINAL NAME: $ORIGINAL_NAME"
tmp=$(mktemp)
jq '.name = "psg-hub"' "$PKG" > "$tmp" && mv "$tmp" "$PKG"
```

Record ORIGINAL_NAME in SUMMARY.

Avoid: jq with --in-place flag without tmpfile (atomicity matters). Avoid: changing other fields. Avoid: removing dependencies.
  </action>
  <verify>
`jq -r '.name' /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/package.json` returns "psg-hub". `jq 'has("dependencies")' package.json` returns true. `jq '.scripts | keys | length' package.json` returns > 0.
  </verify>
  <done>AC-4 satisfied: name renamed; rest preserved.</done>
</task>

<task type="auto">
  <name>Task 6: pnpm install at workspace root + smoke build</name>
  <files>/Users/schoolcraft_mbpro/apps/psg/pnpm-lock.yaml (NEW), /Users/schoolcraft_mbpro/apps/psg/node_modules/, /Users/schoolcraft_mbpro/apps/psg/apps/psg-hub/node_modules/</files>
  <action>
```bash
cd /Users/schoolcraft_mbpro/apps/psg
pnpm install
pnpm -r list
pnpm typecheck --filter=psg-hub 2>&1 | tail -30
pnpm build --filter=psg-hub 2>&1 | tail -50
```

Capture full output to SUMMARY. If typecheck or build fails:
- Record the failure exit code + last 30 lines of output in SUMMARY
- Do NOT roll back the merge — failures here are recoverable in 01-05.x or next phase
- Flag deferred issues if missing env vars or new TypeScript errors surface

Avoid: `npm install` (project uses pnpm). Avoid: deleting pnpm-lock.yaml if generated. Avoid: bumping any dependency versions (use BSM's locked versions as-is).
  </action>
  <verify>
`/Users/schoolcraft_mbpro/apps/psg/pnpm-lock.yaml` exists and > 0 bytes. `pnpm -r list` shows psg-hub. `pnpm typecheck --filter=psg-hub` exit 0 (or failure recorded). `pnpm build --filter=psg-hub` exit 0 (or failure recorded).
  </verify>
  <done>AC-5 satisfied: workspace has lockfile + recognizes psg-hub; build smoke captured (success or recorded failure).</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE

- `apps/psg/apps/psg-hub/.paul/{PROJECT,ROADMAP,STATE}.md` + `paul.json` + `phases/01-workspace-consolidation/01-0*-PLAN.md` — psg-hub's fresh PAUL is authoritative
- `apps/psg/apps/psg-hub/README.md` — v7 graduation README preserved
- `apps/psg/apps/psg-hub/.git/` — fresh git from graduation
- `apps/psg/projects/**` — SEED PLANNING.md immutable (except `.paul-bridge/01-05-pre-scan.md`)
- `apps/psg/{psg-data-lake, psg-advantage-portal, psg-import, api-psghub}/**` — untouched
- `~/apps/projects/bsm/{studio, integrations, onboarding, preview, shops}/**` — siblings stay for 01-06
- `~/apps/projects/bsm/.paul/**` — original BSM PAUL stays at source (only a copy goes to references/)
- `~/apps/ads/**`, `~/apps/projects/local_reach/**` (if not yet relocated), `~/apps/gbrain/`, `~/apps/open-design/`
- `~/apps/_psg-archive/**`, `apps/psg/archive/**` — 01-02/01-03/01-04 territory

## SCOPE LIMITS

- No upgrades to BSM dependencies — use the locked versions as-is. Upgrades (if any) live in their own plan post-merge.
- No design system token swap — that's 01-Phase 2 (v0.1 milestone Phase 2).
- No Vercel re-link — that's v0.1 Phase 3.
- No new env var values in .env.local — only `.env.example` carries over.
- No removal of BSM siblings' source — 01-06 handles them.
- No psg-hub PAUL `STATE.md` updates beyond the standard PAUL `update_state` step.
- No `.env` files copied even if pre-scan finds them.

</boundaries>

<verification>

Before declaring plan complete:
- [ ] Pre-scan log written
- [ ] Operator approval captured
- [ ] BSM PAUL absorbed into `.paul/references/bsm/`
- [ ] BSM dashboard pre-cleaned (no node_modules / .next / .turbo / .vercel / .git / .env-with-values)
- [ ] BSM dashboard contents merged into psg-hub
- [ ] psg-hub `.paul/` top level UNCHANGED (mtime check against pre-scan)
- [ ] psg-hub `README.md` UNCHANGED
- [ ] psg-hub `.git/` UNCHANGED
- [ ] `~/apps/projects/bsm/dashboard/` returns ENOENT
- [ ] `~/apps/projects/bsm/studio/` still exists (sibling preservation check)
- [ ] `package.json` name renamed to "psg-hub"; original name recorded
- [ ] `apps/psg/pnpm-lock.yaml` generated
- [ ] `pnpm -r list` shows psg-hub workspace member
- [ ] `pnpm typecheck --filter=psg-hub` outcome recorded
- [ ] `pnpm build --filter=psg-hub` outcome recorded

</verification>

<success_criteria>

- All 6 tasks + 1 checkpoint complete
- All 6 acceptance criteria met
- BSM dashboard is now the psg-hub anchor app with workspace-recognized package.json
- BSM PAUL history preserved as reference
- Workspace lockfile exists and psg-hub builds (or build failure is captured for follow-up)

</success_criteria>

<output>
After completion, create `.paul/phases/01-workspace-consolidation/01-05-SUMMARY.md` capturing: pre-scan summary, conflict resolution decisions, BSM PAUL file count absorbed, pre-clean bytes freed, .env files dropped (if any), original BSM package.json name, pnpm install duration, typecheck exit code, build exit code, build output tail (last 30 lines), any deferred issues surfaced.
</output>
