# PAUL Handoff

**Date:** 2026-06-04 (end of day)
**Status:** paused — mid-phase (Phase 9, plan 09-02 not yet started)

---

## READ THIS FIRST

You have no prior context. This tells you everything.

**Project:** psg-hub — unified PSG platform (customer portal + internal ops + agentic intel) on `hub.psgweb.me`.
**Core value:** one branded role-gated surface replacing PSG's fragmented tooling.

**Working repo note (changed today):** `psg-hub` is now its OWN git repo → `github.com/Phoenix-Solutions-Group/psg-hub` (`psg-hub.git`), at `~/dev/psg/internal/psg-hub/`. It was split out of the `psg-internal` monorepo so commits don't overlap. Siblings also split: `psg-import` → `import.git`, `sitemap-maker` → `sitemap-maker.git`. `psg-internal` main (`e156e5b`) gitignores all three. **`cd` anywhere under `psg-hub/` and git targets `psg-hub.git`.** The Next app is at `psg-hub/apps/psg-hub/`.

---

## Current State

**Version:** 0.2.0 released (paul.json tracks 0.3.0 in-progress)
**Milestone:** v0.3 Customer Analytics — 🚧 IN PROGRESS, **0 of 4 phases**
**Phase:** 9 of 12 — Analytics foundation + SEMrush
**Plan:** 09-01 ✅ LOOP CLOSED (Phase 9 = 1/3 plans)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY          (09-01 — closed)
  ✓        ✓        ✓
```

**⚠️ v0.3 was RE-PLANNED today** (grounding overturned the ROADMAP premise — same pattern as Phase 6). The old plan assumed "BSM Phase 5 Google Ads data exists, no OAuth, surface it first." Verified FALSE on prod `gylkkzmcmbdftxieyabw`: `google_ads_*` tables ABSENT, no source has stored data, ads page is a coming-soon guard, no chart lib. Re-ordered by ascending OAuth/ingest friction, 3→4 phases:
- **9** Analytics foundation + SEMrush (lowest-friction source; no per-shop OAuth)
- **10** Google Ads (provision tables + wire the already-built OAuth/sync code)
- **11** GA4 + GSC (new per-shop Google OAuth)
- **12** PSG report (multi-LLM narrative + monthly PDF)

---

## What Was Done (this session)

1. **Repo split** — psg-hub / psg-import / sitemap-maker each → own remote; psg-internal untracked them (main `e156e5b`).
2. **Milestone v0.3 created → re-planned** to 4 phases (premise overturn). ROADMAP/STATE/PROJECT/paul.json/phase-dirs synced.
3. **Phase 9 research** (3 parallel agents → `RESEARCH.md` + `research/*.md`): SEMrush HTTP API contract (verified live), Recharts on Next16/React19/TW4, LCP gating.
4. **09-01 ✅ built + closed — all gates green, ZERO prod write:**
   - **Data model — EXTEND not create (grounding):** `analytics_snapshots` ALREADY existed on prod (`shop_id + location_id + date + metrics jsonb`, 0 rows, already RLS-secured with membership-scoped select+insert). Migration `supabase/migrations/20260604000000_analytics_snapshots.sql` EXTENDS it source-agnostic: `add source/period/synced_at` + idempotency `unique(shop_id, source, date, period)` + indexes. **LOCAL-applied only** (`db reset` exit 0); no RLS change.
   - **Helpers:** `src/lib/analytics/{types.ts,snapshots.ts}` — idempotent `upsertSnapshots` (service-role), RLS-clamped `getSnapshots`, `SemrushMetrics` jsonb shape. 6 unit tests.
   - **Charts:** `recharts@3.8.1` + `react-is@19.2.4`; `src/components/analytics/charts.tsx` (`LineChartCard`/`BarChartCard`/`Sparkline`) — `var(--chart-N)` (0 raw hex), `role="img"`+aria, `min-h`, `isAnimationActive={false}`, empty states. 6 chrome tests (recharts mocked — Recharts 3 emits no SVG in node SSR).
   - `vitest.config.ts`: `snapshots.ts` added to coverage include; `charts.tsx` excluded (DOM/recharts → E2E).
   - Gates: db reset 0 · typecheck clean · lint 0 err (1 pre-existing) · **267 tests, 89.2%, perFile≥70** · build ✓.

---

## What's In Progress / Uncommitted

- **09-01 code is UNCOMMITTED** in the `psg-hub.git` repo working tree (per the build-all / pause-at-gates mode — commit was deferred to the phase-end gate batch). Files: `supabase/migrations/20260604000000_analytics_snapshots.sql`, `src/lib/analytics/{types.ts,snapshots.ts,__tests__/snapshots.test.ts}`, `src/components/analytics/charts.tsx` + `__tests__/charts.test.tsx`, `vitest.config.ts`, `package.json`/lockfile (recharts), `.paul/phases/09-*`.
- If a WIP commit was made at pause, see STATE Session Continuity for the SHA/branch.

---

## What's Next

**Immediate:** `/paul:plan` **09-02** — analytics dashboard shell + MSO cross-shop aggregate + switcher search/typeahead + **LCP<2s Playwright gate** + real chart render + axe AA. Use `research/lcp-gating.md` (PerformanceObserver, `storageState: OWNER`, CPU throttle 4x, median, prod build) and `research/recharts-integration.md`.

**After that:** 09-03 SEMrush ingest (use `research/semrush-api.md` — verified contract: `domain_rank` Or/Ot/Oc + `backlinks_overview` total/`score`(NOT ascore) + `domain_organic` Po bucketed; CSV `;`-sep, parse-by-header, ERROR-token sniff; domain = shop URL stripped to root) → `upsertSnapshots` with `source='semrush'`.

**Then the GATE BATCH (single pause, operator):** apply migration to prod (PROTOCOL-migration-safety.md + advisor baseline+diff) · add prod `SEMRUSH_API_KEY` · resolve the `.vercel` link (links at both psg-internal root AND psg-hub repo — psg-hub is now its own repo) · `vercel --prod` · visual/brand human-verify · commit + push to psg-hub.git.

---

## Open Flags

- **Phase-12 report template is GONE** — `archive/local_reach-outputs` (tracys reports) didn't survive the repo split; not on disk. It is the spec for the monthly PDF + the data-model's typed columns. **Nick must re-supply before Phase 12.** Phase 9 proceeds fine on the jsonb model.
- **`.vercel` deploy-from-where** — psg-hub now its own repo; resolve at the deploy gate.
- **shops.url** = 4 of 7 shops have a URL (the per-shop SEMrush key); 3 → no-data state.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (read first) |
| `.paul/ROADMAP.md` | v0.3 4-phase plan + re-plan rationale |
| `.paul/phases/09-analytics-foundation-semrush/09-PHASE-SPLIT.md` | Phase 9 plan split |
| `.paul/phases/09-analytics-foundation-semrush/RESEARCH.md` | 3-agent research (SEMrush/Recharts/LCP) |
| `.paul/phases/09-analytics-foundation-semrush/09-01-SUMMARY.md` | 09-01 outcome + deviations |
| `supabase/migrations/20260604000000_analytics_snapshots.sql` | the extend migration (LOCAL-applied) |
| `src/lib/analytics/snapshots.ts` | snapshot read/write helpers |
| `src/components/analytics/charts.tsx` | Recharts brand primitives |

---

## Resume Instructions

1. Read `.paul/STATE.md` (latest position).
2. Confirm you are in the `psg-hub.git` repo (`~/dev/psg/internal/psg-hub/apps/psg-hub`).
3. `/paul:resume` or `/paul:progress` → then `/paul:plan 09-02`.
4. Keep the mode: build-all vs LOCAL Supabase (08-04b harness, zero prod), PAUSE at the gate batch.

---

*Handoff created: 2026-06-04 (end of day)*
