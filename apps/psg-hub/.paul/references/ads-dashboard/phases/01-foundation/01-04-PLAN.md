---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - app/(authed)/c/[slug]/page.tsx
  - app/(authed)/layout.tsx
  - components/kpi-card.tsx
  - components/last-synced.tsx
  - lib/dummy-data.ts
autonomous: false
---

<objective>
## Goal
Build the first real branded view of the app: a hardcoded `/c/wallace` dashboard rendered from `lib/dummy-data.ts`. This page proves the entire foundation works end-to-end — auth gates it, PSG tokens style it, layout + typography express the anti-slop pillars, and the page passes the `/impeccable critique` exit gate that closes Phase 1.

## Purpose
Phase 1's "it works" moment. Sign in via magic link → land on /c/wallace → see a PSG-identity dashboard with placeholder KPIs that reads like a story, not a metrics dump. This is the visual contract Phases 2-5 implement against.

## Output
- `app/(authed)/c/[slug]/page.tsx` — single client dashboard, hardcoded to read from dummy-data for slug "wallace"
- `components/kpi-card.tsx` — narrative-first KPI card (sentence + metric + delta), token-driven
- `components/last-synced.tsx` — small "last synced" timestamp component
- `lib/dummy-data.ts` — synthetic data for wallace (spend, clicks, conversions, CPL, CTR, sparkline points)
- Authed layout shell updated with minimal top-bar (PSG mark, signed-in user, sign-out)
- `/impeccable critique` exit gate passed (binding) — closes Phase 1
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/SPECIAL-FLOWS.md

## Prior Work
@.paul/phases/01-foundation/01-02-SUMMARY.md
@.paul/phases/01-foundation/01-03-SUMMARY.md

## Sources
- PSG tokens: `tokens/psg.json` (from 01-02)
- Auth pattern: `lib/supabase/server.ts`, `app/(authed)/layout.tsx` (from 01-03)
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| `/impeccable shape` | required (BLOCKING) | Before Task 1 — design brief locked first | ○ |
| `/impeccable craft` | required | During Tasks 2-3 | ○ |
| `/ui-ux-pro-max` | required | Component patterns + accessibility | ○ |
| `/impeccable critique` | required (BLOCKING) | Exit gate of plan AND Phase 1 | ○ |

**BLOCKING:** Both `/impeccable shape` (before code) and `/impeccable critique` (before merge) MUST happen. No exceptions — Phase 1 doesn't close without this gate.
</skills>

<acceptance_criteria>

## AC-1: Shape brief locked before any UI code
```gherkin
Given the page will be a client-facing brand artifact
When `/impeccable shape` runs with the dashboard brief
Then a written shape brief exists (in this PLAN's commit or in .paul/phases/01-foundation/01-04-SHAPE.md)
And it specifies: information hierarchy, primary action, layout strategy, KPI card anatomy, states (default, empty, error), interaction model, content requirements
And no UI code is written before the brief is locked
```

## AC-2: KPI card is narrative-first
```gherkin
Given a KPI value, a delta, and a context sentence
When the KPI card renders
Then the sentence is the most prominent text on the card ("Your ads got 1,247 clicks last month — up 23% from October")
And the raw metric (1,247) is secondary visually
And the delta has accessible color + icon coding (not color-only)
And the card uses PSG tokens exclusively — no hardcoded colors or sizes
```

## AC-3: /c/wallace renders behind auth with PSG identity
```gherkin
Given a signed-in user with role='psg_admin' or client_id matching wallace
When they navigate to /c/wallace
Then the page renders with PSG typography and palette
And shows a header (Wallace Collision Center + last-synced timestamp)
And renders 4-6 KPI cards in a non-grid editorial layout (no 4-up shadcn card grid)
And shows a sparkline panel and a placeholder note timeline
And the entire page reads as a story, not a metrics dump
```

## AC-4: Critique passes the binding gate
```gherkin
Given /c/wallace, /login, and / all expressing PSG identity
When `/impeccable critique` runs against the dashboard
Then the score passes the threshold
And anti-slop pillars 1-8 are not violated
And the critique notes (if any) are either resolved or explicitly deferred to Phase 4 with rationale
```

## AC-5: Phase 1 exit gates clear
```gherkin
Given Plans 01-01, 01-02, 01-03, 01-04 are all complete
When the phase exit checklist runs
Then: tokens locked, auth working end-to-end, demo page renders, critique passing, AEGIS guardrails clean, no AC-N regressed from prior plans
And ROADMAP.md shows Phase 1 = Complete
```

</acceptance_criteria>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Approve the /impeccable shape brief for /c/wallace before any UI code is written</decision>
  <context>
    The dashboard is the visual contract for Phases 2-5. Anti-slop pillars are binding. A locked shape brief prevents drift during implementation.
  </context>
  <options>
    <option id="approved">
      <name>Shape brief approved — proceed to implementation</name>
      <pros>Locks information hierarchy + states + KPI card anatomy before code</pros>
      <cons>None</cons>
    </option>
    <option id="revise">
      <name>Revise the brief</name>
      <pros>Catch issues before code lands</pros>
      <cons>Adds an iteration cycle</cons>
    </option>
  </options>
  <resume-signal>Select: approved or revise (with notes)</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Generate shape brief via /impeccable shape</name>
  <files>.paul/phases/01-foundation/01-04-SHAPE.md</files>
  <action>
    Invoke `/impeccable shape` with this dashboard brief:

      Page: /c/wallace (and /c/[slug] generally)
      Audience: PSG admin or signed-in client user for Wallace Collision Center
      Mood: confident, calm, premium. Reads like a quarterly letter from a senior strategist, not a metrics export.
      Information hierarchy:
        1. Headline KPI card: spend + a sentence summarizing the period
        2. Three secondary KPI cards (clicks, conversions, CPL)
        3. A sparkline panel showing spend over the last 30 days
        4. A placeholder "What we did" timeline section (3 stub entries, hardcoded)
      Primary action: none yet (read-only). Future: "view campaigns" CTA in Phase 4.
      Layout strategy: editorial, single column max-width ~960px, intentional asymmetry, generous whitespace. NO 4-up card grid.
      States to design now:
        - Default (signed-in, data renders)
        - Empty (no data yet — first sync hasn't run; will surface in Phase 2 but design now)
        - Error (sync failed; design now)
        - Loading (skeleton states; design now)
      Content: every KPI card has a one-sentence narrative, not just a number.
      Constraints: anti-slop pillars 1-8 binding. Tokens only.

    Write the shape output as `.paul/phases/01-foundation/01-04-SHAPE.md`.
    Halt before writing any UI code until the brief is approved (checkpoint above).
  </action>
  <verify>
    test -f .paul/phases/01-foundation/01-04-SHAPE.md
    # The brief is human-readable, structured, covers the sections above
  </verify>
  <done>AC-1 satisfied: shape brief locked.</done>
</task>

<task type="auto">
  <name>Task 2: Build KpiCard + LastSynced components</name>
  <files>components/kpi-card.tsx, components/last-synced.tsx</files>
  <action>
    Invoke `/impeccable craft` and `/ui-ux-pro-max` while building.

    1. `components/kpi-card.tsx`:
       Props: { sentence: string, metric: string, delta?: { direction: 'up'|'down'|'flat', percent: number, ariaLabel: string }, sparkline?: number[] }
       Layout:
         - Sentence is the dominant visual element (large body weight, comfortable line-height)
         - Metric is secondary (mid-size display weight, muted compared to sentence)
         - Delta is small, with both an icon and a color (success/danger token); aria-label provides text
         - Optional sparkline rendered inline (Tremor Sparkline or a tiny inline SVG)
       Use PSG tokens for everything — no hex, no raw sizes outside the type scale tokens.
       Accessibility: role/semantics correct, color is not the sole indicator of delta direction.

    2. `components/last-synced.tsx`:
       Props: { at: Date | string | null }
       Renders: "Synced 2h ago" with a tiny status dot (green/amber based on staleness threshold).
       Use a small Intl.RelativeTimeFormat formatter, no external dep.

    Avoid:
    - Adding a "card grid" wrapper here — the page composes the layout
    - Color-only delta indicators
    - Hardcoded hex / px values outside tokens
  </action>
  <verify>
    pnpm typecheck && pnpm lint
    Visual smoke: render in Storybook-equivalent (just import into / for now) and confirm tokens apply
  </verify>
  <done>AC-2 satisfied: KPI card narrative-first, token-driven, accessible.</done>
</task>

<task type="auto">
  <name>Task 3: Build /c/[slug]/page.tsx + authed shell + dummy data</name>
  <files>app/(authed)/c/[slug]/page.tsx, app/(authed)/layout.tsx, lib/dummy-data.ts</files>
  <action>
    1. `lib/dummy-data.ts`:
       - One export: `getDummyClientData(slug: string)` returning a typed object with: client display name, last-synced timestamp, headline + 3 secondary KPIs (each with sentence + metric + delta + sparkline), 3 timeline note stubs
       - Hardcoded "wallace" entry. Other slugs return null (page should 404).

    2. `app/(authed)/c/[slug]/page.tsx` (server component):
       - `params.slug` → call getDummyClientData → if null, notFound()
       - Compose page: header (client name + LastSynced), headline KPI (Card), 3 secondary KPIs (in an editorial layout — NOT a card grid; try one large + two stacked, or asymmetric 2-column), sparkline panel, timeline section with placeholder copy
       - Use generous spacing tokens, varied density

    3. Update `app/(authed)/layout.tsx`:
       - Add a minimal top-bar: PSG wordmark left, user email + sign-out button right
       - Wordmark uses the PSG display font
       - Top bar should NOT feel like a generic shadcn header — strip default borders, use brand spacing
       - Provide a max-width container in the children slot

    Avoid:
    - Falling back to a 4-up card grid for the secondary KPIs
    - Generic "Welcome, user!" copy
    - Adding navigation beyond sign-out (no sidebar — that's Phase 3 when there are multiple clients)
  </action>
  <verify>
    pnpm dev → sign in → navigate to /c/wallace → page renders with all sections
    pnpm typecheck && pnpm lint && pnpm build
  </verify>
  <done>AC-3 satisfied: /c/wallace renders behind auth with PSG identity, editorial layout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    /c/wallace dashboard rendered with PSG tokens, narrative-first KPI cards, editorial layout, behind magic-link auth. This is the Phase 1 visual contract.
  </what-built>
  <how-to-verify>
    1. Open the Vercel preview (or production) URL for the latest deploy
    2. Visit /login, request a magic link with your nick@phoenixsolutionsgroup.net email
    3. Click the link, confirm you land on the authed shell
    4. Visit /c/wallace
    5. Run `/impeccable critique` against the rendered page
    6. Confirm: PSG identity is unmistakable; no AI-slop tells (gradient blobs, 4-up grids, decorative iconography, generic copy); states (empty/loading/error) are designed in source even if not all rendered
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 1, or describe issues to fix before the critique gate</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `tokens/psg.json` (01-02)
- Supabase migration + hook (01-03)
- Auth client/server utilities (01-03)
- `.paul/**`, PLANNING.md, README.md

## SCOPE LIMITS
- Dummy data ONLY — no Supabase reads, no real metrics — Phase 2
- No PSG admin home `/` with all-clients grid — Phase 3
- No real timeline note CRUD — Phase 4
- No PDF export — Phase 5
- Don't add more shadcn primitives unless the page actually needs them (Button + Card from 01-02 should cover it)

</boundaries>

<verification>
Before declaring plan AND Phase 1 complete:
- [ ] `pnpm typecheck && pnpm lint && pnpm build` clean
- [ ] Production deploy serves /login, /c/wallace
- [ ] Magic-link auth works end-to-end on production
- [ ] /c/wallace renders all required sections
- [ ] KPI card narrative-first verified
- [ ] No raw hex/px outside tokens
- [ ] `/impeccable critique` passing score (binding gate)
- [ ] AEGIS guardrails clean
- [ ] All 5 ACs satisfied
- [ ] ROADMAP.md updated: Phase 1 = Complete; v0.1 milestone progress reflects it
</verification>

<success_criteria>
- All 4 tasks completed (1 shape + 2 build + 1 human-verify checkpoint)
- AC-1 through AC-5 satisfied
- Critique gate cleared
- Phase 1 closes — Phase 2 (Data Pipeline) is unblocked
</success_criteria>

<output>
After completion, create `.paul/phases/01-foundation/01-04-SUMMARY.md` AND a phase-level `.paul/phases/01-foundation/PHASE-SUMMARY.md` recording:
- Final shape-brief decisions
- Critique score + any items deferred to Phase 4 with rationale
- Production URL with screenshots (link or attach)
- Open questions handed forward to Phase 2 (e.g., schema specifics for `client` table now that we've stubbed it)
</output>
