# PAUL Handoff

**Date:** 2026-06-11
**Status:** paused — 12-04 APPLY mid-checkpoint (Stage F live smoke pending one secret)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** psg-hub — unified PSG platform (customer analytics + monthly report).
**Core value:** the automated PSG monthly client report (multi-LLM narrative + branded PDF), the core v0.3 output.

---

## Current State

**Version:** 0.3.0 (milestone v0.3 Customer Analytics)
**Phase:** 12 of 12 — PSG report
**Plan:** 12-04 — monthly cron + operator activation. APPLY in progress, autonomous=false.

**Loop Position (12-04):**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ◑        ○      (APPLY: Tasks 1-2 done; Task 3 gate batch A-E done, F pending)
```

NOTE: 12-04 is NOT the last plan — **12-05** (GA4-dimensional + real-perf via CrUX/PageSpeed/GTMetrix) follows and closes Phase 12 + milestone v0.3. 12-05 is research-gated. See ROADMAP 12-05 line + memory `project-psg-report-ga4-perf-expansion`.

---

## What Was Done (this session)

**12-04 code (Tasks 1-2) — built + green, ZERO prod contact:**
- `src/lib/report/monthly.ts` — pure deps-injected `runMonthlyReports(period, deps)` (generate→eval→storeNarrative→render→storePdf→record→email; idempotent, per-shop fault-contained, hold=no-email). Tests `__tests__/monthly.test.ts` (5).
- `src/app/api/cron/monthly-report/route.ts` — CRON_SECRET-gated cron + 503 not-configured guard; wires real bindings (gatewayGenerate, render-client, storage, sendEmail, service shop/owner-email loaders); period = prior month. Test `__tests__/monthly-route.test.ts` (4).
- `vercel.json` — appended monthly cron `0 0 1 * *` (5 crons total; 4 daily untouched).
- Gates: tsc 0 · eslint 0 · vitest **532** green.

**12-04 Task 3 operator gate batch — Stages A–E DONE + verified on PROD:**
- **A** migration applied to `gylkkzmcmbdftxieyabw` via Supabase MCP — `public.monthly_reports` (PK shop_id+period_month) + member-SELECT RLS + storage.objects SELECT policy for the bucket. Advisor diff byte-identical to baseline (zero new advisories). Verified: table RLS on, policies present.
- **B** private Storage bucket `monthly-reports` created (public=false).
- **C** Hetzner worker LIVE: CPX21, Ashburn, **IP 178.156.192.225**, server id 139686862. Docker `report-renderer` container up (restart=always), Caddy TLS, ufw 22/80/443. **https://render.psgweb.me/health = ok** (external HTTPS verified). cloud-init had a Caddy conffile-prompt error (worker unaffected); fixed manually (dpkg --configure + caddy started).
- **D** 5 Vercel prod envs set: `RENDER_TOKEN`, `REPORT_RENDER_URL=https://render.psgweb.me`, `NEXT_PUBLIC_APP_URL=https://hub.psgweb.me` (was MISSING in prod), `AI_GATEWAY_API_KEY`, `REPORT_EMAIL_TEMPLATE_ID=d-f9c7d831133347bbaf0e87eae04c1507`.
- SendGrid dynamic template created via API: **`d-f9c7d831133347bbaf0e87eae04c1507`** (handles shopName/monthLabel/reportUrl, branded).
- **E** deployed: `vercel --prod` from REPO ROOT (rootDirectory=apps/psg-hub) → `dpl_9Jtwocy8S7rG6EmyQdvL6N3bPb4U` READY, aliased hub.psgweb.me. Verified: cron route 401 unauth (gate works), worker health ok.

---

## What's In Progress / Blocked

**Stage F (live smoke) — BLOCKED on one secret:** `CRON_SECRET` is marked Sensitive in Vercel (un-pullable). Need the operator to paste it (or rotate it — precedented in 11-04, but needs another redeploy + changes the 4 daily crons' shared secret).

---

## What's Next

**Immediate (resume Stage F):** with `CRON_SECRET` in hand:
```bash
curl -X POST https://hub.psgweb.me/api/cron/monthly-report -H "Authorization: Bearer <CRON_SECRET>"
```
Then verify the chain (Supabase MCP project `gylkkzmcmbdftxieyabw`):
- PDF object at `monthly-reports/{wallace_shop_id}/2026-05.pdf` (period = prior month at run time).
- `select * from monthly_reports` row with storage_path + emailed_at set.
- Owner link-email received; the download link returns 200 for the logged-in owner, 403 for a non-member.
- If counts = held (no linked sources for the month) or failed, capture the JSON + diagnose. Wallace has GA4+GSC+Ads data; expect a sent.

**After smoke:** finish 12-04 APPLY → `/paul:unify 12-04` (writes 12-04-SUMMARY.md; does NOT run the milestone transition — 12-05 closes the phase). Then `/paul:plan 12-05` (RESEARCH FIRST — CrUX/PageSpeed/GTMetrix new APIs; prefer ultracode Workflow).

**Branch:** all 12-04 work uncommitted on `feature/12-psg-report`. Per-plan commit pending (commit on request). Final phase merge→main deferred to 12-05 close (12-04-GATE-BATCH.md Stage G).

---

## SECURITY — rotate after close (all pasted in chat history)

- **Hetzner API token** — rotate in Hetzner console.
- **AI Gateway key** (`vck_…`) — rotate in Vercel AI Gateway.
- **SendGrid API key** (`SG.…`) — rotate in SendGrid.

Local secret files (chmod 600, gitignored locations, NOT in repo):
`~/.psg-hetzner-token` · `~/.psg-render-token` · `~/.psg-ai-gateway-key` · `~/.psg-sendgrid-key` · `~/.psg-report-template-id` · SSH key `~/.ssh/psg-report-renderer` (root@178.156.192.225, use `-o IdentitiesOnly=yes`).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/phases/12-psg-report/12-04-PLAN.md` | The plan being executed |
| `.paul/phases/12-psg-report/12-04-GATE-BATCH.md` | Operator activation runbook (A–G) |
| `src/lib/report/monthly.ts` | The orchestrator (built) |
| `src/app/api/cron/monthly-report/route.ts` | The cron route (deployed) |

---

## Resume Instructions

1. Read `.paul/STATE.md` (Session Continuity).
2. Get `CRON_SECRET` from the operator.
3. Run the Stage F smoke (above) + verify the chain via Supabase MCP.
4. `/paul:unify 12-04`, then `/paul:plan 12-05` (research-first).

*Handoff created: 2026-06-11*
