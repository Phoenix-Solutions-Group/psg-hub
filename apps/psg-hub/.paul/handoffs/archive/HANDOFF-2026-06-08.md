# PAUL Handoff

**Date:** 2026-06-08 (EOD pause)
**Status:** paused — Phase 9 ✅ COMPLETE, OPERATOR GATE BATCH pending (prod activation)

---

## READ THIS FIRST

You have no prior context. This tells you everything.

**Project:** psg-hub — unified PSG platform (customer portal + internal ops + agentic intel) on `hub.psgweb.me`.
**Core value:** one branded role-gated surface replacing PSG's fragmented tooling.

**Working repo:** `psg-hub` is its OWN git repo → `github.com/Phoenix-Solutions-Group/psg-hub` (`psg-hub.git`), at `~/dev/psg/internal/psg-hub/`. Next app at `psg-hub/apps/psg-hub/`. `cd` anywhere under `psg-hub/` → git targets psg-hub.git.

---

## Current State

**Version:** 0.2.0 (paul.json tracks 0.3.0 in progress)
**Milestone:** v0.3 Customer Analytics — 🚧 IN PROGRESS, **1 of 4 phases ✅**
**Phase:** 9 of 12 — Analytics foundation + SEMrush — **✅ COMPLETE (3/3 plans loop-closed)**

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY          (Phase 9 — all 3 plans closed; transition done)
  ✓        ✓        ✓
```

---

## What Was Done (Phase 9, this + prior session)

- **09-01** analytics data model — EXTENDED pre-existing `analytics_snapshots` (source/period/synced_at + idempotency key) + recharts@3.8.1 brand chart primitives. LOCAL migration only.
- **09-02** `/dashboard/analytics` surface — per-shop + MSO all-shops aggregate + designed empty/loading/error states + "Last synced"; switcher typeahead at ≥8 shops; LCP Playwright gate; real-browser chart render + axe AA. Aggregation proven in E2E (982=491+491).
- **09-03** SEMrush ingest — contract-correct client (parse-by-returned-header, ERROR sniff, `score`-not-`ascore`, fail-loud `SemrushContractError`, `redactApiKey`, retry+breaker) + `syncSemrushSnapshots` (daily idempotent rows, url-less skipped, per-shop failure contained) + `analytics_sync_runs` ledger migration (LOCAL) + `CRON_SECRET`-gated cron route + vercel.json daily 06:00 UTC.
- Transition: PROJECT/ROADMAP/STATE/paul.json evolved; phase commit `cb65992` on `feature/09-analytics` (LOCAL, **not pushed**).

**Gates (all green, local):** 322 unit tests (semrush ~98%) · 10/10 E2E · typecheck clean · lint 0 err · build ✓ · ZERO prod contact · zero live SEMrush calls (fetch mocked everywhere).

---

## What's In Progress / Uncommitted

- Nothing uncommitted in the tree — Phase 9 is committed (`cb65992`) on `feature/09-analytics`, ahead of origin by 1 (the 09-01 wip `9f2f266` is already pushed).
- **The work has NEVER run against real SEMrush or prod** — by design (no key in build). First live cron tick at the gate batch is that test.

---

## What's Next — ⚠️ OPERATOR GATE BATCH (the single Phase-9 pause; ALL outward-facing — nothing ran without operator)

1. Review the whole-phase diff on `feature/09-analytics` (`9f2f266` + `cb65992`)
2. **Prod migrations ×2** under `PROTOCOL-migration-safety.md` + advisor baseline/diff EACH:
   - `supabase/migrations/20260604000000_analytics_snapshots.sql` (incl. the 09-02 `location_id drop not null` amendment)
   - `supabase/migrations/20260605000000_analytics_sync_runs.sql` (ledger)
3. **Prod secrets ×2:** `SEMRUSH_API_KEY` + `CRON_SECRET` (cron route 401s/locked without CRON_SECRET)
4. `.vercel` link resolution (links at BOTH psg-internal root AND psg-hub repo) → `vercel --prod`
5. **First-live-run verify = REAL numbers on /dashboard/analytics for the 4 url-bearing shops** (NOT "cron returned 200"). The contract guard fails loud on full header mismatch; a partial rename could still zero ONE metric — if any reads 0 unexpectedly, log the raw response header line.
6. Visual/brand human-verify of the analytics surface
7. Merge `feature/09-analytics` → main + push psg-hub.git

**After the gate batch:** `/paul:plan` **Phase 10 — Google Ads** (provision the absent `google_ads_*` tables + wire the already-built OAuth/sync code + paid panel into the Phase-9 shell; needs operator Google OAuth creds + a pilot-shop link).

---

## Open Flags / Carry

- **shops.url = 4 of 7** have a URL (the per-shop SEMrush target); 3 → designed no-data state.
- **LCP hard-budget promotion:** /dashboard/analytics is on a 4000ms calibration ceiling (measured 84ms — LCP element is server-streamed text, so chart hydration is unmeasured). Promote to HARD 2000ms when next touched, or add a chart-specific probe.
- **Phase-12 report template GONE** off-disk (`archive/local_reach-outputs`) — Nick re-supplies before Phase 12.
- Deferred backlog: Stripe/billing + PII-retention → v0.4; audit-log → v1.5; full 14-domain AEGIS → v2.0.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state (read first) |
| `.paul/ROADMAP.md` | v0.3 4-phase plan; Phase 9 ✅ |
| `.paul/phases/09-analytics-foundation-semrush/09-03-SUMMARY.md` | Gate-batch checklist (Next Phase Readiness) |
| `src/lib/semrush/{client,sync}.ts` | SEMrush ingest |
| `src/app/api/cron/semrush-sync/route.ts` | Cron trigger (CRON_SECRET-gated) |
| `src/app/dashboard/analytics/page.tsx` | The analytics surface |
| `supabase/migrations/20260604000000_*.sql`, `20260605000000_*.sql` | The two prod-pending migrations |

---

## Resume Instructions

1. Read `.paul/STATE.md` (Session Continuity).
2. Confirm in the `psg-hub.git` repo (`~/dev/psg/internal/psg-hub/apps/psg-hub`, branch `feature/09-analytics`).
3. `/paul:resume` → either run the GATE BATCH with the operator (each prod action approved) or `/paul:plan` Phase 10 if the operator deferred activation.

---

*Handoff created: 2026-06-08 (EOD)*
