# PAUL Handoff

**Date:** 2026-06-03
**Status:** paused — mid-APPLY (08-02 at the final human-verify checkpoint)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** psg-hub — unified PSG platform (customer portal + internal ops + agentic intel) on Next.js 16, shared Supabase prod `gylkkzmcmbdftxieyabw`, LIVE at hub.psgweb.me.
**Core value:** consolidate fragmented PSG tooling into one role-gated branded surface.

---

## Current State

**Milestone:** v0.2 Customer MVP (🚧, 2 of 3 phases done)
**Phase:** 8 of 8 — Launch hardening (v0.2 phase 3 of 3)
**Plan:** 08-02 — blanket-allow RLS remediation — **APPLY in progress, 3 of 4 tasks PASS, awaiting operator human-verify**

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ◑        ○      (APPLY: T1-T3 done; T4 human-verify pending)
```

---

## What Was Done This Session

1. **Authored 08-02-PLAN.md** (complex, autonomous=false). Grounding reframed D3: the deferred policies are blanket `Allow all` for BOTH `anon` AND `authenticated` on 12 multi-tenant tables = cross-tenant breach (any logged-in user reads/writes every shop's rows) = the literal Phase-8 blocker. Not just "26 anon-open."
2. **Operator split 08-02** → 08-02 (RLS only) + **08-02b** (gitleaks v0.2 + S4 idempotency, light, planned at its slot). ROADMAP + STATE updated.
3. **APPLY 08-02 T1 (checkpoint:decision) RESOLVED** — operator chose **deny-all-9 + profiles option A**. Evidence: the 9 sibling/agentic tables empty/stale (no writes since Mar 20), zero psg-hub refs, consuming apps retired. Recorded to STATE `### Decisions`.
4. **APPLY T2 — migration applied to prod.** `supabase/migrations/20260603194623_close_blanket_allow_rls.sql` (migrations-as-code, `db push` clean). Dropped 24 blanket policies (anon+auth) on 12 tables; `shops` survivors retightened `{public}`→`{authenticated}`; `profiles` self-row SELECT/UPDATE + `psg_superadmin` read; `clients` + 9 siblings → default-deny.
5. **APPLY T3 — verification PASS.** Advisor diff: `rls_policy_always_true` 26→2, **zero new ERROR/WARN** (ERROR steady 6; WARN 74→50 = −24 dropped; INFO +10 = intended default-deny tables). RLS impersonation proof: customer (5d6a) sees 1 shop / 1 profile / 0 clients; Nick (superadmin) 1 shop / 4 profiles (intended option-A read); anon 0/0/0. Gates: typecheck clean, 229 tests, build green.

---

## What's In Progress

- **08-02 Task 4 (checkpoint:human-verify)** — awaiting operator. Migration is ALREADY live on shared prod (RLS is DB-side; no redeploy needed — same DB as the running hub.psgweb.me app). Operator needs to log into hub.psgweb.me as a member (`nmschoolcraft@gmail` = Tracy's owner, or Demo Body Shop owner) and confirm dashboard/settings/ads/reviews render with no 500 / no empty-shop regression.

---

## What's Next

**Immediate:** resume APPLY at Task 4 — get operator "approved" on the live member-flow check (or fix any reported breakage; classify intent/spec/code first). Server-side proof already shows the breach closed + member reads scoped, so member flows are expected intact.

**After approval:** `/paul:unify .paul/phases/08-launch-hardening/08-02-PLAN.md` → write 08-02-SUMMARY.md, close the loop.

**After 08-02 closes:** 08-02b (gitleaks + S4 idempotency), then 08-03 (first AEGIS pass), 08-04 (quality gates S5).

**Housekeeping (carried from 08-01):** branch `phase-8/08-01-carry-in` holds 08-01 (on prod ahead of merge) and now 08-02's uncommitted work too. Merge to main when convenient. Nothing is committed yet this session.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state — see APPLY 08-02 execution log + `### Decisions` |
| `.paul/phases/08-launch-hardening/08-02-PLAN.md` | The plan being executed |
| `supabase/migrations/20260603194623_close_blanket_allow_rls.sql` | The applied migration (inline rollback SQL in header comment) |
| `.paul/phases/06-rbac-rls-spine/PROTOCOL-migration-safety.md` | Binding S1 gate (migrations-as-code, advisor diff) |
| `.paul/phases/06-rbac-rls-spine/CHECKLIST-rls-review.md` | Per-table RLS review checklist |

---

## Resume Instructions

1. Read `.paul/STATE.md` (Current Position + APPLY 08-02 execution log + Session Continuity).
2. If operator approves the live check → run `/paul:unify .paul/phases/08-launch-hardening/08-02-PLAN.md`.
3. If operator reports a broken flow → classify (intent/spec/code) before patching; the RLS change is the only variable.

**Rollback if needed:** the migration header comment has exact `create policy "Allow all for ..."` statements to restore any dropped policy.

---

*Handoff created: 2026-06-03*
