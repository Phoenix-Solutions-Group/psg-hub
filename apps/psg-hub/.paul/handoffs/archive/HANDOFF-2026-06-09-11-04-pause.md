# PAUL Handoff

**Date:** 2026-06-09 EOD
**Status:** paused — APPLY 11-04 in progress, stopped at the Task 3 operator checkpoint (prod activation not yet run)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** psg-hub — one branded `hub.psgweb.me` surface (customers + internal + superadmin), role-gated.
**Core value:** Replaces PSG tooling/login sprawl with unified role-gated access.

---

## Current State

**Version:** 0.3.0
**Milestone:** v0.3 Customer Analytics — IN PROGRESS (1 of 4 phases fully closed; Phase 11 build done, activation pending)
**Phase:** 11 — GA4 + GSC
**Plan:** 11-04 GATE-BATCH (prod activation) — APPLY in progress

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ◐        ○
```
APPLY: Task 1 (author runbook) ✓ · Task 2 (auto: fix + branch + commit) ✓ · Task 3 (checkpoint:human-action, operator runs prod) PENDING.

---

## What Was Done (this session)

- **Task 2 auto complete.** Branched `feature/11-ga4-gsc` off `main` and landed ONE Phase-11 commit `cf4591f` ("GSC daily ingest (11-03) + Phase-11 activation runbook (11-04)"). **Local only — NOT pushed.**
- Commit carries the 11-03 GSC ingest surface (gsc-client/metrics/sync + cron + "Search performance" panel + e2e) and the 11-04 runbook/plan docs.
- **AC-2 verified fresh:** gsc-metrics passes siteUrl RAW (no `encodeURIComponent` — googleapis@173 RFC-6570-encodes the `{siteUrl}` path param itself; the manual encode double-encoded → 404/403). tsc 0 · gsc tests 13/13.
- Tracking synced: `paul.json` loop → APPLY; STATE loop marker → ◐ with the deviation recorded.

**DEVIATION (recorded):** Plan AC-3 wanted ONE commit of the "11-01+02+03 trees." Reality: 11-01/11-02 already committed AND pushed in `798a41a` on `origin/main` (before this session). So `cf4591f` carries only the 11-03/11-04 remainder. The branch still holds the FULL Phase-11 build for runbook Stage C — no history rewrite, intent preserved.

---

## What's In Progress

- **Task 3 — operator gate batch (NOT started).** The prod activation runbook `.paul/phases/11-ga4-gsc/11-04-GATE-BATCH.md` is authored and waiting. Nothing has touched prod: no migration applied, no secret set, no deploy, no OAuth link.

---

## What's Next

**Immediate:** Operator executes `11-04-GATE-BATCH.md` against prod, in order:
- **Stage 0 (operator-only):** consent publish for the 2 new SENSITIVE scopes (`analytics.readonly` + `webmasters.readonly`) — empirical >7-day token gate, not an ETA; enable GA4 Admin/Data + GSC API; confirm a pilot shop has a GA4 property AND a verified GSC site (no site → GA4 live + GSC activation-pending, honest partial).
- **Stage A (Claude drives via Supabase MCP on authorization):** apply the TWO 11-01 migrations under PROTOCOL (advisor baseline → apply ONE → diff → ABORT on unintended diff). Expected: `google_oauth_accounts` RLS + 1 SELECT policy · `google_oauth_pending_states` RLS-on/0-policy = one `rls_enabled_no_policy` INFO.
- **Stage B:** `vercel env add GOOGLE_ANALYTICS_OAUTH_REDIRECT_URI production` (5 others already set, NO dev token).
- **Stage C:** push `feature/11-ga4-gsc` + `vercel --prod` from repo toplevel `/dev/psg/internal/psg-hub` (confirm `.vercel` rootDirectory `apps/psg-hub/`; do NOT deploy from the above-repo `.vercel` HAZARD).
- **Stage D:** link a pilot Google account (OAuth click = operator-only), trigger both crons with `CRON_SECRET`, READ the live surface.

**Pass-gate (loop closes on REAL NUMBERS, not cron-200):** GA4 non-zero sessions/users/key_events AND GSC non-zero clicks/impressions/position — OR the honest GA4-live / GSC-pending partial.

**After that:** `/paul:unify 11-04` (write 11-04-SUMMARY) → Phase 11 transition (PROJECT/ROADMAP evolve, Phase 11 → Complete + LIVE) → `/paul:plan` Phase 12 (PSG report).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (loop position + full narrative) |
| `.paul/phases/11-ga4-gsc/11-04-GATE-BATCH.md` | THE operator runbook to execute (6 stages, real-number pass-gates) |
| `.paul/phases/11-ga4-gsc/11-04-PLAN.md` | The plan (3 tasks / 4 ACs) |
| `.paul/phases/11-ga4-gsc/11-03-SUMMARY.md` | What the GSC vertical shipped |
| `.paul/phases/10-google-ads/10-03-GATE-BATCH.md` | The proven template this mirrors (executed live 06-09) |

---

## Git / Deploy Facts

- Branch `feature/11-ga4-gsc` @ `cf4591f` — **local only, not pushed.** `main` == `origin/main` @ `798a41a` (carries 11-01/11-02).
- Repo: `Phoenix-Solutions-Group/psg-hub.git`. Toplevel `/dev/psg/internal/psg-hub`. `.vercel` rootDirectory `apps/psg-hub/`. HAZARD: a 2nd `.vercel` exists ABOVE the repo — deploy from toplevel only.
- prod-on-main auto-deploy is OFF (`vercel.json git.deploymentEnabled.main=false`); prod ships via CLI `vercel --prod`.
- Working tree intentionally dirty: `.paul/STATE.md` + `paul.json` + `ROADMAP.md` (tracking, evolve at transition) and untracked root `docs/` (unrelated FileMaker-Advantage spec — DO NOT stage).

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position.
2. The auto work is done + committed (`cf4591f`). Resuming = driving Task 3 (the operator runbook).
3. Start at Stage 0; Claude drives any CLI/MCP step (migrations, `vercel env`, deploy, cron triggers) on operator authorization. Consent/API-enable/GSC-verify/OAuth-click are operator-only.
4. On real numbers (or the honest partial) → `/paul:unify 11-04`.

---

*Handoff created: 2026-06-09 EOD*
