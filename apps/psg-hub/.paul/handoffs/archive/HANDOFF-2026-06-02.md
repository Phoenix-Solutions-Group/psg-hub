# PAUL Handoff

**Date:** 2026-06-02
**Status:** paused (mid-PLAN, plan-check in-flight)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** psg-hub — unified PSG platform (customer portal + internal ops + agentic intel) in one Next.js 16 app, separated by RBAC + tier + RLS. Live at `hub.psgweb.me`.
**Core value:** Consolidates fragmented PSG tooling into one branded, role-gated surface for customers, staff, and superadmins.

**Before doing anything for Phase 6, read `.paul/phases/06-rbac-rls-spine/06-RESEARCH-DOSSIER.md` in full.** It is the authoring source and it **overrides the ROADMAP's Phase-6 assumptions, which were proven wrong (W1–W6).**

---

## Current State

**Version:** 0.2.0 (milestone v0.2 Customer MVP — 🚧 in progress; v0.1 ✅ complete + tagged)
**Phase:** 6 of 8 — RBAC + RLS spine (v0.2 phase 1 of 3)
**Plan:** 06-01 — **authored, NOT approved.** Awaiting (a) operator fork decisions and (b) plan-check fixes.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY        (plan 06-01)
  ◑        ○        ○
```
PLAN is half-done: 06-01-PLAN.md exists but Task 1 is a blocking `checkpoint:decision` the operator hasn't answered, and the adversarial plan-check had not returned when paused.

---

## What Was Done (this session)

- Ran a **read-only grounding workflow** (run `wf_ddd01605-c64`, 6 agents, ~707k tokens, zero DDL) against the live shared Supabase project `gylkkzmcmbdftxieyabw`. Output saved as `06-RESEARCH-DOSSIER.md`.
- **Falsified the ROADMAP's Phase-6 premise at six points (W1–W6):** no role enum (free-text, zero CHECK) → must DEFINE not extend; `security_profiles` is greenfield; the **BSM Phase-4 RLS base was never deployed** here (psg-hub code reads phantom `shop_members`/`google_ads_*`/`agent_runs` → those customer surfaces error at runtime today); `profile_id`==`user_id`==`auth.uid()` (no W4 conflict); migrations are tracked remotely (5 rows) but no in-repo files; live admins = Nick/Tina/**Claire**, **Brian has no auth row**. Real model = `shop_users(user_id,shop_id,role,location_ids[])` + helpers `user_shop_ids()`/`user_location_ids()`/`user_is_shop_owner()` (local_reach/psg-import lineage, not BSM).
- Established the **blast-radius verdict**: customer DDL lands on a shared prod DB with **314,828 PII rows / 142 shops**; default-deny RLS is safe (one cosmetic regression that only ever served Nick); the only thing forcing "isolate" is a project-global token hook — which D+E recommend NOT installing.
- **Authored `06-01-PLAN.md`** (decision + migrations-as-code linkage + S1/S4 protocol docs; **changes NO schema**) and laid out **06-02/06-03 as a provisional split table**.
- Updated `STATE.md` (Current Position, Loop, Phase 6 Plan Split, Decisions, Session Continuity).

---

## What's In Progress

- **Adversarial plan-check of 06-01 — DONE + APPLIED.** 2 lenses (executability + safety/fidelity), both PASS_WITH_FIXES. All 12 findings folded into `06-01-PLAN.md`. The two HIGHs (both verified against the Supabase CLI docs): `supabase db pull` writes ONE `_remote_schema.sql` snapshot (not 5 reconstructed files), and its default flow INSERTs a baseline row into the remote `schema_migrations` table — so the plan now declines that prompt, forbids `--yes`/inline-password, and verifies the remote still shows exactly 5 versions (true zero-prod-write). D3 reframed as a fixed "defer to Phase 8"; `private` helper schema named; advisor baseline dated + 06-02 re-captures. No further plan revision needed. (Raw result: `/private/tmp/claude-501/-Users-schoolcraft-mbpro-apps-psg/e6919c8a-b3bd-425e-b832-2000787c55cd/tasks/wghbstocb.output`.)

---

## What's Next

**Immediate:**
1. Present the **D1–D5 operator forks** (06-01 Task 1) and get decisions. Recommended coherent path: **D2 no-hook security-definer subquery → D1 shared-mutate (A1) → D3 defer the 26 anon policies to Phase 8 → D4 per-shop grain → D5 vestigial `profiles.role`.** Also ask: Brian's exact superadmin email + Claire (`claire@static-solutions.com`) disposition.

**After that:**
3. `/paul:apply .paul/phases/06-rbac-rls-spine/06-01-PLAN.md` (links repo to remote migrations + commits the S1/S4 protocol; no DDL).
4. Then plan + apply **06-02** (RBAC + RLS DB spine — the actual role tables/helpers/bootstrap, gated on the fork outcomes) and **06-03** (middleware customer-id gate + reconcile phantom-table app code to `shop_users`).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/phases/06-rbac-rls-spine/06-RESEARCH-DOSSIER.md` | **Authoring source — read first.** Grounded reality + blast radius + forks + split. |
| `.paul/phases/06-rbac-rls-spine/06-01-PLAN.md` | The authored plan (decision + migrations-as-code + protocol; no DDL). |
| `.paul/STATE.md` | Live state (see Current Position, Phase 6 Plan Split, Decisions, Session Continuity). |
| `.paul/ROADMAP.md` | Phase overview — **note its Phase-6 assumptions are superseded by the dossier (W1–W6).** |

---

## Resume Instructions

1. `/paul:resume` (or read `.paul/STATE.md` → Session Continuity).
2. Read `06-RESEARCH-DOSSIER.md` before touching Phase 6.
3. Plan-check is already done + applied — go straight to: surface the D1–D5 forks to the operator, then `/paul:apply .paul/phases/06-rbac-rls-spine/06-01-PLAN.md`.

**Hard boundaries (carry forward):** 06-01 applies NO schema change; never drop the 26 anon-open policies in Phase 6 (→ Phase 8); never install a project-global token hook unless path-B is chosen; never touch the `sensitive` schema or sibling-app tables.

---

*Handoff created: 2026-06-02*
