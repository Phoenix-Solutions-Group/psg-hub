# PAUL Handoff

**Date:** 2026-06-13
**Status:** paused — v0.3.5 created, Phase 13 RESEARCHED, ready for /paul:plan

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** psg-hub — unified PSG platform for collision-repair shops (~842). Customer portal: marketing analytics + the automated monthly PSG client report. Next.js 16, Supabase Postgres (shared prod `gylkkzmcmbdftxieyabw`), Vercel Fluid Compute. LIVE at hub.psgweb.me.
**Core value:** one branded `hub.psgweb.me` surface replacing tooling sprawl, role-gated.

---

## Current State

**Version:** 0.3.5 (milestone v0.3.5 Presence + Sentiment, 🚧 in progress, 0 of 2 phases)
**Phase:** 13 of (13,14) — GBP presence foundation + insights — NOT STARTED, research done
**Plan:** none yet — research-first gate satisfied, ready for `/paul:plan` Phase 13

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○      (Phase 13 ready for first PLAN; RESEARCH ✓)
```

---

## What Was Done (this session)

1. **Closed 12-05c (the last v0.3 plan) ACTIVATION-PENDING.** Build-local wiring (getMonthlySnapshot + readers into the PRINT path; two cron routes ga4-dims-sync/perf-sync; vercel.json 5→7). Gate-batch ran THROUGH Stage B on prod (both migrations applied, advisor zero-delta; 3 secrets set; deploy dpl_FEDz4AE6mWxrydVcJHQ9Gsb2h7kn READY, 7 crons live; Wallace shops.url set). Stage C live smoke SKIPPED — CRON_SECRET is Vercel-Sensitive/un-pullable and the operator declined the rotation a manual trigger needs. 12-05c-SUMMARY.md written.
2. **Closed milestone v0.3 Customer Analytics (4/4 phases).** /paul:unify 12-05c + transition + /paul:complete-milestone: MILESTONES.md entry, `.paul/milestones/v0.3.0-ROADMAP.md` archive, ROADMAP collapsed, PROJECT/STATE/paul.json evolved, package.json 0.2.0→0.3.0, annotated tag `v0.3.0` (LOCAL, unpushed). Merged `feature/12-psg-report` → main: merge `37359c8` + close commit `5415522`, both PUSHED to origin/main.
3. **Created milestone v0.3.5 Presence + Sentiment** (/paul:discuss-milestone + /paul:milestone). Operator picked v0.3.5 over the slated v0.4, scope = GBP insights + review read/respond + LLM sentiment on Google reviews. ROADMAP/STATE/paul.json updated; phase dirs `13-gbp-presence` + `14-reviews-sentiment` created; MILESTONE-CONTEXT.md consumed.
4. **Phase 13 RESEARCH done** (ultracode Workflow `wf_9f94f2c1-01b`, 10 agents, 2 adversarial verdicts) → `.paul/phases/13-gbp-presence/13-RESEARCH.md` (294 lines, repo-grounded).

---

## What's In Progress / Next

**Immediate:** `/paul:plan` Phase 13 (GBP presence foundation + insights). Research-first gate is SATISFIED.

**The research reshaped the plan — two corrections a planner MUST honor:**
1. **The "reuse the Phase-11 token" premise is dead.** The existing consent grants only `analytics.readonly` + `webmasters.readonly` (verified in `authorize/route.ts:12-13`). It carries no `business.manage`, so every GBP call 403s. Every linked shop (incl. Wallace) must RE-CONSENT.
2. **No Testing-mode escape.** The OAuth app is already In Production (Phase 10), so adding `business.manage` re-triggers sensitive-scope verification, and tokens can be revoked 7 days post-consent while unverified. Even the PILOT is activation-pending, not just the fleet.

**Build shape (from research):** two `analytics_snapshots` sources — `'gbp'` daily (FLOW → promote into the `AnalyticsSource` union → gets panel/report/rollup free) + `'gbp_presence'` monthly (STOCK → SnapshotSource-only, like `performance`/`ga4_dimensions`). THREE CHECK-widening migrations (analytics_snapshots, analytics_sync_runs, google_oauth_accounts). Two cron orchestrators cloned from `ga4-dims-sync`/`perf-sync`. Recommended link flow = a SEPARATE GBP consent (own google_oauth_accounts row, small token blast radius). Wiring idiom = `google.auth.OAuth2` injection (like GSC), NOT the gax authClient (GA4). googleapis@173 ships typed v1 clients for all three Phase-13 APIs. Pull the GBP star rating into the presence row so the panel isn't anemic.

**TWO Google lead-time gates (file/check on day 1, parallel with build-local):**
- **Gate A** — Business Profile API access request. Project quota = 0 QPM until approved (everything errors), flips to 300 QPM. ~14-day Google review. Form: `support.google.com/business/contact/api_default`. Per-GCP-project (no transfer). CHECK the GCP Quotas page first — it may already be approved.
- **Gate B** — OAuth sensitive-scope verification for `business.manage`. OPEN: confirm it's sensitive, not restricted (restricted = a CASA Tier-2 assessment, weeks + paid). Re-verification effort for adding one scope to an already-verified app is unconfirmed.

Phase 13 will most likely close ACTIVATION-PENDING until the empirical 7-day-token pass-gate holds for the pilot (same build-local → operator-gate pattern as Phases 9-12).

---

## SECURITY — act on this

- **A Google Cloud API key was pasted in chat this session: `***REMOVED-GCP-API-KEY***`.** It is NOT usable for GBP (GBP is OAuth-only) and was NOT stored anywhere. Treat as compromised → **delete it** in GCP → APIs & Services → Credentials (or rotate + restrict to Places API if keeping it for public rating lookups later).
- **v0.3 chat-pasted secrets still owed rotation:** `PAGESPEED_API_KEY` (`AIza…`) + `GTMETRIX_API_KEY` (12-05c) + Hetzner token / AI Gateway key (`vck_`) / SendGrid key (`SG.`) (12-04 carry).
- CRON_SECRET is Vercel-Sensitive (un-pullable); rotate only if a manual cron trigger is needed.

---

## Standing v0.3 follow-ups (NOT v0.3.5-blocking)

- **⭐ July 1** — the `0 5 1 * *` monthly-report cron + the new `0 2`/`0 3` GA4-dims/perf ingest crons fire. The scheduled agent `trig_01G7MfA382AUXYTYXnc5Knvk` (2026-07-01 14:00Z) verifies SEND, not section correctness. The 12-05 build-blind parsers (GA4 `totals[0]`, PSI `loadingExperience`, GTMetrix `/reports/{id}`) get their FIRST live run unmonitored → verify the Wallace July PDF renders the 4 GA4 dimensional sections + Website-performance block. 12-04 found two prod bugs at this exact gate.
- Optional: `git push origin v0.3.0` to publish the milestone tag.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (Session Continuity is current) |
| `.paul/ROADMAP.md` | v0.3.5 Current Milestone + Phase 13/14 detail |
| `.paul/phases/13-gbp-presence/13-RESEARCH.md` | The Phase-13 research dossier (read before planning) |
| `.paul/MILESTONES.md` | v0.3 closed entry |
| `src/lib/google-oauth/` | Phase-11 OAuth foundation to reuse (accounts.ts, client.ts, gsc-client.ts idiom) |
| `src/lib/analytics/{snapshots,types,rollup}.ts` | analytics_snapshots model + the SnapshotSource vs AnalyticsSource discriminator |

---

## Git State

On `main`, in sync with origin. v0.3 merged + pushed (37359c8, 5415522); v0.3.5 scaffold + research + this handoff committed `26cd29f` (pushed). `docs/` is an unrelated FileMaker spec — never stage it. Tag `v0.3.0` is local/unpushed. Vercel `git.deploymentEnabled.main=false` so pushes don't auto-deploy (prod = CLI deploy dpl_FEDz4).

**⚠️ DEPLOY GAP — operator commit landed on main mid-session (`1249a21`, by Nick, 2026-06-13 16:00):** `fix(12-psg-report): atomic send claim + renderer SSRF guard + force wiring`. Hardens the LIVE report pipeline: an atomic send-claim (new migration `supabase/migrations/20260613000000_monthly_reports_claim.sql` + `monthly.ts` + `monthly-report/route.ts` + tests) and a renderer SSRF URL-allowlist (`workers/report-renderer/url-allowlist.mjs` + `render.mjs`). **Status to confirm next session:** (1) is `20260613000000_monthly_reports_claim.sql` APPLIED to prod `gylkkzmcmbdftxieyabw`? (2) has main been DEPLOYED (`vercel --prod`) since 12-05c, or does prod still run `dpl_FEDz4` (pre-hardening)? Both matter before the July 1 send — the new route code expects the claim table/columns, so deploying the code without applying the migration would break the monthly-report cron. If prod stays on `dpl_FEDz4`, July 1 fires the pre-hardening pipeline (no SSRF guard, no atomic claim) — decide whether to deploy+migrate the hardening before July 1.

---

## Resume Instructions

1. Read `.paul/STATE.md` (Session Continuity + Blockers/Concerns).
2. Read `.paul/phases/13-gbp-presence/13-RESEARCH.md`.
3. Run `/paul:plan` Phase 13 (research-first gate already satisfied).
4. In parallel: file/check Gate A in the GCP project; delete the pasted API key.

*Handoff created: 2026-06-13*
