# PAUL Handoff

**Date:** 2026-06-08
**Status:** paused (meetings) — APPLY mid-loop, blocked on an operator checkpoint

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** psg-hub — unified PSG platform (customer portal + internal ops + agentic intel) on one branded `hub.psgweb.me` Next.js 16 app, role/tier/RLS-separated.
**Core value:** replace PSG tooling sprawl with one role-gated surface.

---

## Current State

**Version:** v0.3 Customer Analytics (🚧 in progress; 1 of 4 phases complete)
**Phase:** 10 of 12 — Google Ads
**Plan:** 10-03 — APPLY **paused at the checkpoint:human-action** (the operator gate batch)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓       ⏸        ○      (Tasks 1+2 done; Task 3 = operator checkpoint, waiting)
```

---

## What Was Done (this session)

- `/paul:plan 10-03` — authored `10-03-PLAN.md`. Confirmed COMBINED Phase-9+10 scope with the operator (one branch / one deploy ships both — nothing from either phase is activated yet). Research-first gate ✓ (RESEARCH.md `wf_a78f4fd7-d6b` has a dedicated 10-03 section). Advisor pre-write pass resolved loop-closure = real-numbers, not runbook-authored.
- `/paul:apply 10-03` **Task 1** — authored `.paul/phases/10-google-ads/10-03-GATE-BATCH.md`: the operator runbook (Stage 0 lead-time → A SEMrush → B Google Ads → C merge/push; per-migration advisor-PROTOCOL inline; real-numbers pass-gates).
- **Task 2** — local commit `8f0527c` of the 10-02 working tree + the 10-03 docs. **NOT pushed** (ahead 6 of origin/main).
- Verified + pinned the real deploy facts (corrected stale STATE): repo `psg-hub.git`, deploy from repo toplevel, rootDirectory `apps/psg-hub/`, duplicate `.vercel` hazard, main auto-deploy OFF. 3 migrations confirmed at `apps/psg-hub/supabase/migrations/`.

---

## What's In Progress

- **10-03 APPLY is paused at Task 3** (checkpoint:human-action). The runbook is written and committed locally; the operator must now execute it against prod. The loop stays open until real numbers are reported. UNIFY has NOT run.

---

## What's Next

**Immediate (operator):** work `.paul/phases/10-google-ads/10-03-GATE-BATCH.md` end to end:
1. **Stage 0 (start first — days of lead time):** dev-token ≥ Explorer in `ads.google.com/aw/apicenter` (Basic ~2 biz-day review); OAuth consent screen → **In Production** (else `adwords` refresh tokens revoke at 7 days).
2. **Stage A (SEMrush):** 2 migrations under PROTOCOL → `SEMRUSH_API_KEY` + `CRON_SECRET` → `vercel --prod` from `/dev/psg/internal/psg-hub` → real numbers on `/dashboard/analytics` for the 4 url-shops.
3. **Stage B (Google Ads):** `google_ads_tables` migration → 6 Google secrets + redeploy → pilot OAuth link (`status='linked'`) → real paid metrics (single-row / non-zero / account-tz / bare 10-digit customer_id).
4. **Stage C:** merge `feature/09-analytics` → main → push.

**After that:** report results → `/paul:unify 10-03`. Honest close — Phase 10 live only on real numbers for BOTH sources; if Google lead-time blocks Ads, Phase 9 closes live and Ads = activation-pending. Then Phase 11 (GA4 + GSC).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state (Session Continuity at the bottom) |
| `.paul/phases/10-google-ads/10-03-GATE-BATCH.md` | **The runbook the operator executes** |
| `.paul/phases/10-google-ads/10-03-PLAN.md` | The 10-03 plan (authoring + checkpoint) |
| `.paul/phases/10-google-ads/10-02-SUMMARY.md` | The Ads ingest + paid panel being activated |
| `.paul/phases/10-google-ads/RESEARCH.md` | GAQL contract + the 10-03 live-verification list |
| `.paul/ROADMAP.md` | Phase 10 map + v0.3 overview |

---

## Resume Instructions

1. Read `.paul/STATE.md` Session Continuity.
2. Open `.paul/phases/10-google-ads/10-03-GATE-BATCH.md` — that is the operator's runbook.
3. When the operator reports gate-batch results (real numbers / activation-pending / a PROTOCOL abort), run `/paul:unify 10-03` to close the loop.
4. Local commit `8f0527c` holds all work; nothing is pushed. Push/merge/deploy are the runbook's Stage-C operator steps.

---

*Handoff created: 2026-06-08*
