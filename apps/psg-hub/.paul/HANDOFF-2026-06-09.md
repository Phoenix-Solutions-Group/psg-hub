# PAUL Handoff

**Date:** 2026-06-09
**Status:** APPLY mid-loop (10-03). Phase 9 LIVE on prod. Phase 10 blocked on an MCC code gap — operator decision pending.

---

## READ THIS FIRST

Continuation of the 10-03 gate batch (`HANDOFF-2026-06-08.md`). The operator (Nick) worked the runbook live this session with Claude driving the automatable steps. Phase 9 (SEMrush) is now genuinely live on prod with real numbers. Phase 10 (Google Ads) infra is deployed but the pilot link is blocked by a real product gap (MCC unsupported).

**Project:** psg-hub — unified PSG platform on `hub.psgweb.me` (Next.js 16). Repo `Phoenix-Solutions-Group/psg-hub.git`, toplevel `/Users/schoolcraft_mbpro/dev/psg/internal/psg-hub`, app at `apps/psg-hub/`.

---

## What got done this session (ALL on prod)

### Stage A — Phase 9 SEMrush: ✅ LIVE
- Prod migrations applied under PROTOCOL (advisor baseline→diff, each clean, only expected `rls_enabled_no_policy` deltas):
  - `20260604000000_analytics_snapshots.sql` (extend + location_id nullable). Verified: source CHECK admits semrush/google_ads/ga4/gsc, location_id nullable, 0 rows.
  - `20260605000000_analytics_sync_runs.sql` (ledger, RLS on / 0 policies).
- Secrets set (Vercel production): `SEMRUSH_API_KEY`, `CRON_SECRET`.
- Deployed; cron `/api/cron/semrush-sync` → `{"synced":4,"skipped":3,"failed":0}`.
- **REAL NUMBERS** in `analytics_snapshots` (source=semrush, date 2026-06-09): Flower Hill ×3 (traffic 652 / kw 307 / AS 21 / bl 692 — shared domain), Tracy's (1150 / 330 / 12 / 659). All 4 url-shops non-zero. **Phase 9 closes live.**

### Stage B — Phase 10 Google Ads infra: ✅ DEPLOYED (link blocked)
- Migration `20260608000000_google_ads_tables.sql` applied under PROTOCOL. Verified: 4 tables RLS on; accounts/campaigns 1 membership SELECT policy each; oauth_states + ads_api_call_log default-deny. Advisor diff: only `rls_enabled_no_policy` 65→67 (the 2 default-deny tables). Clean.
- 6 secrets set (Vercel production): `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET`, `GOOGLE_ADS_DEVELOPER_TOKEN`, `GOOGLE_ADS_OAUTH_REDIRECT_URI` (= `https://hub.psgweb.me/api/ads/google/callback`), `ADS_STATE_SECRET` (generated), `ADS_ENCRYPTION_KEY` (generated, base64→32 bytes, key_version 1). Redeployed; `/api/cron/google-ads-sync` 503-gate cleared → `200 {"synced":0}`.

### Infra fix (was a real blocker)
- Vercel project `psg-hub` (prj_CBrI1FRqqgPzCbAwin6LbSknY48U) server-side **Root Directory was stale** `psg-hub/apps/psg-hub` (pre repo-split) → `vercel --prod` resolved a doubled nonexistent path. **Fixed via API PATCH → `apps/psg-hub`.** Deploys from repo toplevel now work. (The 06-08 handoff's "rootDirectory apps/psg-hub" was the LOCAL project.json; server overrode it. Nobody had test-deployed until now.)
- Latest prod deploy: `dpl_7YsQ6FCsVUXYaB5k4ayR2gGFh343` (READY, aliased hub.psgweb.me).

### Pilot onboarding (operator chose Wallace)
- Nick logs in (`nick@phoenixsolutionsgroup.net`, uid `27d3990a-1cce-41cc-b027-7a22a1f86df1`) and only had membership to **Demo Body Shop**. Real shops (Tracy's, Flower Hill ×3) had no membership for him and **no `subscriptions` row** (tier gate needs a subscriptions row OR slug in `SHOP_ADS_TIER_OVERRIDE`).
- Operator chose **Wallace Collision (Google Ads acct 604-861-1995)** as the pilot. Wallace was NOT in the hub (`shops.client_id` is NOT NULL → needs a `clients` row). Onboarded minimally (one txn):
  - client `Wallace Collision`, shop `107fa991-4775-49fc-a5c9-f559a5d1550b` (slug `wallace-collision`, subscription_tier performance), `shop_users` row nick=**owner**.
  - Set Vercel `SHOP_ADS_TIER_OVERRIDE=wallace-collision` + redeployed (no fake Stripe row; subscriptions requires NOT-NULL stripe ids).
- redirect_uri_mismatch fixed: Nick added `https://hub.psgweb.me/api/ads/google/callback` to the OAuth client's Authorized redirect URIs.

---

## THE BLOCKER — MCC unsupported (Phase 10 link)

After OAuth consent, the callback (`src/app/api/ads/google/callback/route.ts:120-124`) **hard-errors when `listAccessibleCustomers` returns >1 customer**: "single-customer link only in this release." PSG's Google account is an **MCC (manager)** → sees many customers → **no real PSG client can link as-is.** This is a genuine product gap, not config.

**Operator decision (Nick, this session): "add MCC support for sure" + "the system should allow the user to choose which accounts are visible, at authentication."** → Build an account-selection step in the OAuth link.

### Design sketch (NOT yet built — see open question first)
- Single account per shop (multiple accounts/shop would collide on the `analytics_snapshots (shop_id,source,date,period)` key — a larger ingest change).
- Migration: transient `pending_*` columns on `google_ads_oauth_states` (encrypted token, key_version, scope, login_customer_id, customers[]).
- `callback`: on >1, encrypt token, stash in state row (don't consume), render account-picker page.
- New `POST /api/ads/google/select`: re-bind session (user==state.userId), consume state, validate pick ∈ offered list, persist chosen account with `login_customer_id=<MCC>`.
- `client.ts:204` already passes `login_customer_id` → metrics sync works for MCC children once stored.

### ⚠️ OPEN QUESTION that gates the design (advisor catch + RESEARCH.md:155)
`listAccessibleCustomers` returns customers **directly accessible** to the user. For an MCC login that is often **the manager account(s), NOT the child accounts**. If so, Wallace `6048611995` may not appear in that list and the picker must enumerate children via a `customer_client` GAQL query under `login-customer-id=<MCC>` — a different data source. **VERIFY (context7/Google Ads API docs) before building the picker.** RESEARCH.md line 155 already lists this as open.

### Still needed from Nick
- **PSG MCC / manager customer id** (10-digit) for `GOOGLE_ADS_LOGIN_CUSTOMER_ID` + per-account `login_customer_id`. Confirm Wallace 604-861-1995 is a child of it.

---

## Loop position

```
PLAN ──▶ APPLY ──▶ UNIFY   (10-03)
  ✓       ⏸        ○
```
- Stage A (SEMrush) live. Stage B infra live. Stage B link BLOCKED on MCC.
- Honest close available per the runbook: **Phase 9 live + Google Ads activation-pending.** MCC support = a researched build unit (10-04 / scope addition), not an inline gate-batch hack.

## Recommended next action
1. Decide build-now-inline vs research-unit for MCC (recommend: research-unit — security-sensitive prod OAuth, needs another migration, rests on the open API question; this codebase already shipped one latent token bug, the 10-01 bytea fix).
2. Get the MCC id.
3. Either build MCC support (verify enumeration first) OR `/paul:unify 10-03` closing Phase 9 live + Ads activation-pending.

## Not yet done
- Stage C: merge `feature/09-analytics` → main → push. NOTHING committed/pushed this session (all work is prod-side DB/secrets/deploy + the Wallace data; no code changes yet).
- `/paul:unify 10-03`.

*Handoff created: 2026-06-09*
