# PAUL Handoff

**Date:** 2026-06-18 (session 2 — 12-05c live validation + PSI diagnosability fix)
**Status:** paused (no active loop; 12-05c validation CLOSED, residuals deferred)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** psg-hub — unified PSG platform (customer portal + internal ops + agentic intel) on `hub.psgweb.me`, role-gated by RBAC + tier + RLS.
**Core value:** Consolidates fragmented PSG tooling into one branded surface customers, staff, and superadmins all use.

---

## Current State

**Version:** 0.3.5
**Milestone:** v0.3.5 Presence + Sentiment — ✅ CLOSED 2026-06-17 (NO active milestone)
**Phase:** 12 (psg-report) — its 12-05c parser-validation tail was closed this session
**Plan:** none active

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [no active loop]
```

---

## What Was Done (this session)

The carried-over **12-05c parser validation** (Phase-12 GA4-dims + website-perf parsers were build-blind — never run against live APIs; first scheduled cron is 2026-07-01). Validated LIVE on prod instead of waiting.

1. **Could not run the original handoff plan** — it needed `CRON_SECRET`, which is Vercel write-only/unrecoverable (`env pull` returns it empty) and the operator didn't have it either. With operator approval, **rotated `CRON_SECRET`** to a generated value (in `~/.psg-cron-secret`, chmod 600) + **redeployed** so the manual cron-POST gate would pass. Vercel's scheduled crons auto-use the new value.
2. **Ran both monthly crons on prod** (manual POST, month=2026-05):
   - `ga4-dims-sync` → `{"synced":1,"failed":0}` — Wallace row.
   - `perf-sync` run 1 → `{"synced":4,"failed":1}` — **Wallace threw `PSI HTTP 400`**.
3. **Verified parser OUTPUT via Supabase SQL** (operator pasted `metrics`, since service-role key is also write-only and I can't read rows):
   - **GA4-dims parser ✅ VALIDATED** — full `Ga4DimensionsMetrics`: 4 dimension arrays, `(other)` reconciliation with `engagement_rate` correctly omitted, `averageSessionDuration`=112.57s.
   - **PSI-lab parser ✅ VALIDATED** — full `PsiResult` on Flower Hill (×2) + later Wallace (perf_score 84).
4. **Shipped a diagnosability fix** — `defaultHttpGet` in `src/lib/perf/psi.ts` discarded the PSI error body, so the 400 reason never hit logs. Now throws `PSI HTTP {status}: {body}` (whitespace-collapsed, 500-cap, no api key in body). +1 vitest that drives the real fetch path via a stubbed global `fetch`. Commit **`c104227`**, deployed **`dpl psg-dw0h14jb2`**. Gates: tsc 0 / eslint 0 / vitest **748**.
5. **Re-POSTed `perf-sync`** → `{"synced":5,"failed":0}` — **Wallace succeeded.** The 400 was **TRANSIENT, not a WAF/crawler block** (hypothesis disproven by a clean re-run on identical code). Wallace now has its 2026-05 `performance` row; PSI-lab validated on 3 shops.

Commits this session (all on `main`, **NOT pushed** — 11 ahead of origin):
- `c104227` fix(perf): capture PSI error body in PerfHttpError
- `e885d99` docs(paul): 12-05c validation results + archive 06-18 (session 1) handoff
- `12ca120` docs(paul): Wallace PSI 400 was transient; diagnosability fix shipped

---

## What's In Progress

Nothing active. Three **low-priority residuals** in `.paul/DEFERRED.md`:
- **Make a transient PSI 400 retryable** — `isRetryablePerfError` treats all 4xx as non-retryable, so a flaky Lighthouse 400 loses a shop for the whole month. Branch on the now-captured body (retry a document-request error; keep a genuine bad-request 400 non-retryable). Waits on a real captured 400 body.
- **GTMetrix parser unexercised on prod** — `GTMETRIX_API_KEY` + `GTMETRIX_SHOP_IDS` are configured, but no inspected row (2× Flower Hill + Wallace) carries `gtmetrix` → none is the pilot shop. Inspect the scoped shop's row, else it rides the 2026-07-01 cron.
- **CrUX `psi.field` expected-null fleet-wide** — null even for Wallace (745 sessions); collision-shop origins sit below the CrUX threshold. Non-null branch covered only by unit tests. Informational.

---

## What's Next

**Immediate:** `/paul:discuss-milestone` → v0.4 Invoicing + Payments (or `/paul:milestone` to create directly).

**Time-bound watch (2026-07-01):** the first scheduled monthly crons render Wallace's July PDF + run GTMetrix. Verify the PDF's new GA4-dims + perf sections then. (DEFERRED → Time-bound watch.)

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (Session Continuity at the bottom) |
| `.paul/DEFERRED.md` | Single backlog — 12-05c residuals + security + roadmapped items |
| `src/lib/perf/psi.ts` | PSI fetch + parser (diagnosability fix landed here) |
| `src/lib/google-oauth/ga4-dimensions.ts` | GA4 dimensional parser (validated) |
| `src/lib/analytics/types.ts` | The `metrics` jsonb contracts validated against |

---

## Gotchas (carry forward)

- **Deploy:** `vercel --prod --yes` from the **repo toplevel** `/Users/schoolcraft_mbpro/dev/psg/internal/psg-hub` (root `.vercel` rootDirectory=`apps/psg-hub/`). Running from **inside** `apps/psg-hub` doubles the path and fails.
- **Prod auto-deploy on main is OFF** — commits to main do NOT deploy; CLI `vercel --prod` is the deploy path. `vercel --prod` ships the **working tree**, not git HEAD — `git status` before deploying.
- **Secrets:** prod env vars are Sensitive/write-only — `env pull` returns them empty. `CRON_SECRET` is now a KNOWN value in `~/.psg-cron-secret` (600) — treat that file as a secret. The chat-exposed GBP client secret still needs rotation (DEFERRED.md security).
- **DB reads:** service-role key is also write-only locally → I cannot read `analytics_snapshots`. Row verification needs the operator to run SQL in Supabase Studio and paste `metrics`.
- **Push is operator-gated** — `main` is 11 ahead of origin; outward action, not done here.
- **Peer:** a second Claude session may share this repo — confirm idle before driving `.paul`/shared files.

---

## Resume Instructions

1. Read `.paul/STATE.md` (Session Continuity) + `.paul/DEFERRED.md`.
2. No active loop — go `/paul:discuss-milestone` for v0.4, or pick up a DEFERRED residual.
3. `/paul:resume` or `/paul:progress` for routing.

---

*Handoff created: 2026-06-18 (session 2)*
