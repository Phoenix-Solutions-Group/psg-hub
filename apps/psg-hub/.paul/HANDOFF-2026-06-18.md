# PAUL Handoff

**Date:** 2026-06-18
**Status:** paused (mid-session; one validation task handed to the operator)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** psg-hub — unified PSG platform (customer portal + internal ops + agentic intel) on `hub.psgweb.me`, role-gated by RBAC + tier + RLS.
**Core value:** Consolidates fragmented PSG tooling into one branded surface customers, staff, and superadmins all use.

---

## Current State

**Version:** 0.3.5
**Milestone:** v0.3.5 Presence + Sentiment — ✅ CLOSED 2026-06-17 (no active milestone)
**Phase:** 14 (Reviews + sentiment) — closed; this session added a post-close carried follow-up
**Plan:** 14-03b sentiment surface (read) — ✅ LOOP CLOSED + LIVE on prod

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [14-03b closed; no active loop]
```

---

## What Was Done (this session, 2026-06-18)

All committed to `main`, all LIVE on prod (`dpl psg-2349hrjhp`):

- **`8c8438c`** SOURCE-column fix — Reviews-page "View on Google" link via `metadata.mapsUri` on the gbp_presence snapshot (no migration).
- **`5bd7647`** 14-03b sentiment surface (read) — per-review badges + **Classify-now** + membership-gated `/api/reviews/classify`; per-shop dashboard sentiment panel; additive monthly-report sentiment block; NEW shared `getReviewSentimentSummary`; optional `shopId` on `classifyPendingSentiment`. Merged Platform+Source columns per operator feedback. 14-03b-SUMMARY.md written.
- **`4779a16`** classify-cascade fix — root-caused the live "5 new, 195 failed": the singleton `sentimentBreaker` (threshold 3) cascaded a rate-limit burst into ~190 `CircuitOpenError` fast-fails. Fix = pace calls (150ms) + early-stop on open circuit (remaining defer to next run). Gateway-Haiku path itself verified working (live repro).
- **`9de583f`** consolidated all deferred work into **`.paul/DEFERRED.md`**.
- **`aed92a7`** STATE note (cascade resolved).

Gates throughout: tsc 0 · eslint 0 · vitest 747 · build ✓.

---

## What's In Progress

**12-05c parser validation — handed to the operator, awaiting results.**

The Phase-12 12-05 GA4-dimensional + website-performance parsers were "build-blind" (never run against the live APIs; first scheduled run is the 2026-07-01 cron). The operator chose to validate now. The fetchers can't run locally (all needed secrets are Vercel **Sensitive/write-only** — `env pull` returns them empty), so the test runs on prod via the two cron routes' manual POST (CRON_SECRET gate, prior month = May 2026).

**Operator runs (in their own terminal, CRON_SECRET set locally — keep it out of chat):**
```bash
curl -s -X POST https://hub.psgweb.me/api/cron/ga4-dims-sync -H "Authorization: Bearer $CRON_SECRET"; echo
curl -s -X POST https://hub.psgweb.me/api/cron/perf-sync    -H "Authorization: Bearer $CRON_SECRET"; echo
```
Good signal: each returns `{"month":"2026-05","synced":>=1,...,"failed":0}` (parsers didn't throw).

**Then, to confirm sections are populated (counts alone miss a silent mis-parse), Supabase Studio SQL:**
```sql
select s.source, s.date, s.metrics
from analytics_snapshots s
join shops sh on sh.id = s.shop_id
where sh.name ilike '%wallace%'
  and s.source in ('ga4_dimensions','performance')
  and s.date = '2026-05-01';
```
Verify on resume: ga4_dimensions row has `topChannels/topLandingPages/devices/newVsReturning` + `averageSessionDuration`; performance row has `psi.lab` + `psi.field` (CrUX) + `gtmetrix`. If populated → close the July-1 watch item early.

---

## What's Next

**Immediate:** When the operator pastes the curl JSON + the SQL `metrics`, verify the parsed sections (above). If good, mark the 2026-07-01 watch item resolved in `.paul/DEFERRED.md`.

**After that:** `/paul:discuss-milestone` → v0.4 Invoicing + Payments.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state (Session Continuity at the bottom) |
| `.paul/DEFERRED.md` | All outstanding deferred items (single backlog) |
| `.paul/ROADMAP.md` | Phase/milestone overview (v0.4 queued) |
| `.paul/phases/14-reviews-sentiment/14-03b-SUMMARY.md` | The sentiment-surface plan close |
| `src/lib/reviews/sentiment-summary.ts` | Shared sentiment aggregate (dashboard + report) |
| `src/lib/reviews/review-sentiment-sync.ts` | Classify orchestrator (paced + early-stop) |

---

## Gotchas (carry forward)

- **Deploy:** `vercel --prod --yes` from the **repo toplevel** `/Users/schoolcraft_mbpro/dev/psg/internal/psg-hub` (its `.vercel` has `rootDirectory: apps/psg-hub/`). Running from **inside** `apps/psg-hub` **doubles the path** and fails. An above-repo `../.vercel` is a stale hazard — ignore it.
- **Prod auto-deploy on main is OFF** — commits to main do NOT deploy; CLI `vercel --prod` is the deploy path.
- **Secrets:** prod env vars are Sensitive/write-only — not retrievable via `env pull`. The chat-exposed GBP client secret still needs rotation (see DEFERRED.md).
- **Peer:** a second Claude session (`3cv3ql33`, ttys001) shares this repo — confirm idle before driving `.paul`/shared files.

---

## Resume Instructions

1. Read `.paul/STATE.md` (Session Continuity) + `.paul/DEFERRED.md`.
2. If the operator has pasted the 12-05c curl + SQL output, verify the parsed sections and update the July-1 watch item.
3. Otherwise `/paul:discuss-milestone` for v0.4, or `/paul:progress`.

---

*Handoff created: 2026-06-18*
