services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
  portal:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env.local
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
