/**
 * @deprecated Use PSG_TOKENS from '@/lib/psgTokens' instead.
 * Re-exports canonical tokens so existing imports keep working.
 */
export { PSG_COLORS, EMI_TIER_COLORS, PSG_TOKENS, CHART_DEFAULTS } from './psgTokens'
